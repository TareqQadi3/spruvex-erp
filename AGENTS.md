# AGENTS.md — دليل العمل على مستودع SpruVex

> اقرأ هذا الملف بالكامل قبل أي تعديل. هو المرجع الأول لأي وكيل/نموذج يعمل على هذا المستودع.
> آخر تحديث: 2026-09-03

---

## 1) ما هذا المستودع

مستودع يضم **مشروعين مستقلين تماماً** تشغيلياً (قاعدة بيانات وخادم ونشر منفصلون كلياً):

| المسار | المنتج | التقنيات |
|---|---|---|
| `spruvex-app/` | **SpruVex ERP** — نظام ERP/POS/صيانة متعدد المستأجرين (SaaS) للسوق السعودي، متوافق مع ZATCA | Express + React (Vite) + Drizzle + PostgreSQL (Neon) + pnpm |
| `spruvex-r/` | **SpruVex R** — نظام تشغيل مطاعم مستقل بالكامل | NestJS + Prisma + PostgreSQL (RLS) + Redis + Next.js |

> ⛔ **لا تُشارك أي `.env` أو قاعدة بيانات أو حاوية بين المشروعين إطلاقاً.** كلاهما يستخدم أسماء متغيرات متطابقة (`DATABASE_URL`, `JWT_SECRET`, `REDIS_URL`) بقيم مختلفة كلياً — نسخ ملف بيئة من أحدهما للآخر خطأ أمني حقيقي.

**المواقع التسويقية ليست هنا** — انتقلت إلى مستودع `TareqQadi3/spruvex-s-erp-r`. مجلد `spruvex-site/` الموجود هنا **نسخة مجمّدة** ولا يُعدَّل.

---

## 2) خريطة SpruVex ERP (`spruvex-app/`)

```
spruvex-app/
├── artifacts/
│   ├── api-server/src/
│   │   ├── modules/      ★ البنية الحديثة — 26 موديولاً (كل موديول: routes + service + repository + validators)
│   │   ├── routes/       ⚠ مسارات قديمة (legacy) لم تُرحَّل بعد — منتجات، تصنيفات، صيانة، جلسات نقدية...
│   │   └── core/         مشترك: قاعدة بيانات، كاش، بريد، أخطاء، تسجيل، middleware، طوابير
│   └── pos-system/src/   واجهة React (POS، لوحة تحكم، مخزون، صيانة، محاسبة، تقارير، إعدادات)
├── lib/
│   ├── db/               ★ سكيما Drizzle — المصدر الرسمي الوحيد للسكيما
│   │   ├── src/schema/   تعريف الجداول
│   │   └── drizzle/      26 ملف migration مولَّد
│   ├── api-spec/         مواصفة OpenAPI
│   ├── api-zod/          أنواع zod مولَّدة من المواصفة
│   └── api-client-react/ عميل مولَّد للواجهة
└── .env                  غير متتبَّع في git
```

**الموديولات الحديثة**: accounting, ai, auditLog, auth, bi, branches, customers, ecommerce, expenses, importExport, inventory, invoicing, payments, platform, pos, products, public, purchases, rbac, sales, subscriptions, suppliers, support, sync, vouchers, zatca.

---

## 3) الأوامر

```bash
# ERP — كل الأوامر من داخل spruvex-app/
pnpm install
pnpm run typecheck                                    # فحص الأنواع للمشروع كله
pnpm run build                                        # typecheck + بناء كل الحزم
pnpm -C artifacts/api-server run test                 # اختبارات الوحدة (بلا قاعدة بيانات)
pnpm -C artifacts/api-server run test:integration     # اختبارات تكامل (تحتاج قاعدة بيانات حقيقية)
pnpm -C lib/db run generate                           # توليد migration بعد تعديل السكيما
pnpm -C lib/db run migrate                            # تطبيق الـmigrations

# SpruVex R — من داخل spruvex-r/
pnpm install && pnpm build && pnpm lint && pnpm typecheck && pnpm test
```

**قبل أي دفع (push)**: `typecheck` + `test` + `build` يجب أن تمر جميعها. لدى المستودع CI (`.github/workflows/ci.yml`) يمنع الدمج عند الفشل — لا تحاول الالتفاف عليه.

---

## 4) الخطوط الحمراء — ممنوع تعديلها بلا مراجعة بشرية صريحة

هذه المناطق خطأ واحد فيها = تسريب بيانات بين شركات أو فقدان مال حقيقي، ولن يظهر بالضرورة في أي اختبار حالي:

| المنطقة | لماذا |
|---|---|
| **عزل المستأجرين** — أي استعلام يجب أن يُصفّى بـ`company_id` المأخوذ من الـJWT **فقط** | إسقاط الشرط = رؤية شركة لبيانات شركة أخرى |
| **الصلاحيات (`modules/rbac`، middleware الصلاحيات)** | تجاوزها = وصول كاشير لبيانات مالية |
| **فحص الاشتراك (`requireActiveSubscription`)** | إزالته = استخدام مجاني غير محدود |
| **توقيع فواتير ZATCA وسلسلة التجزئة (`modules/zatca`)** | كسرها = فواتير غير مقبولة نظامياً |
| **بوابات الدفع (`modules/payments`)** خصوصاً `idempotencyKey` والـwebhooks | خطأ = دفعة مزدوجة أو دفعة ضائعة |
| **ملفات migration المطبَّقة فعلاً في `lib/db/drizzle/`** | لا تُعدَّل ولا تُحذف أبداً — أي تصحيح يكون بـmigration **جديد** |
| **محرك المخزون وخصم الكميات (`modules/inventory`, `saleService`)** | خطأ = مخزون خاطئ عند التاجر |

**القاعدة**: إن كان تعديلك يلمس أياً مما سبق — اكتب خطتك أولاً واعرضها للمراجعة، ولا تنفّذ.

---

## 5) قواعد عمل إلزامية

1. **لا حلول مؤقتة.** لا `// TODO: fix later`، لا التفاف على خطأ بإخفائه، لا `any` لإسكات TypeScript. ابحث عن السبب الجذري.
2. **لا تحذف منطق أعمال قائماً** بحجة إعادة الهيكلة. حافظ على المسارات وعقود الـAPI والصلاحيات والسلوك.
3. **لا تعطّل أو تتخطّى اختباراً** لتحصل على "أخضر". اختبار فاشل = مشكلة حقيقية.
4. **كل جدول جديد يجب أن يحوي**: `id` (UUID)، `company_id`، `created_at`، `updated_at`. وفكّر في الحذف الناعم (soft delete) وسجل التدقيق.
5. **لا أسرار في أي ملف متتبَّع.** المفاتيح في `.env` فقط.
6. **ثنائية اللغة**: كل نص واجهة يمر عبر i18n (عربي/إنجليزي) ويدعم RTL/LTR. لا نصوص مكتوبة مباشرة في الكود، ولا مفاتيح ترجمة مكشوفة للمستخدم.
7. **المال بالسنتات (integers)** — لا أرقام عشرية عائمة في أي حساب مالي.
8. **بعد تعديل السكيما**: ولّد migration (`pnpm -C lib/db run generate`) — لا تعدّل قاعدة البيانات يدوياً.
9. **وثّق ما أنجزت** في `SPRUVEX_CURRENT_STATUS.md` ضمن نفس الالتزام — بصدق، شاملاً ما لم يكتمل.
10. **لا تدّعِ نجاحاً لم تتحقق منه فعلياً.** إن لم تُشغّل الاختبار، قل ذلك.

---

## 6) مناطق آمنة للعمل المستقل

مناسبة تماماً للعمل بلا مراجعة مسبقة (مع بقاء قواعد §5):

- صفحات وواجهات جديدة، وتحسينات UX (حالات فارغة، أخطاء، تحميل)
- ترجمات i18n
- كتابة اختبارات جديدة
- تقارير وعروض بيانات تعتمد نقاط API قائمة
- إعادة هيكلة داخل ملف/مكوّن واحد بلا تغيير سلوك
- توثيق

---

## 7) الوثائق المرجعية

| الملف | المحتوى |
|---|---|
| `SPRUVEX_CURRENT_STATUS.md` | ★ حالة المشروع وتاريخ كل مرحلة — **ابدأ منه** |
| `PILOT_LAUNCH_PLAN.md` | خطة الإطلاق التجريبي + المخاطر المعروفة + قائمة الفحص |
| `PRODUCTION_CHECKLIST.md` | قائمة ما قبل الإنتاج |
| `GO_LIVE_RUNBOOK.md` | أوامر النشر الفعلية |
| `INFRASTRUCTURE_DECISIONS.md` | قرارات البنية التحتية |
| `TASKS.md` | ★ قائمة المهام المرتّبة بالأولوية — خذ منها |
| `spruvex-r/README.md` + `spruvex-r/docs/` | توثيق SpruVex R الخاص |

> ⚠️ `spruvex-app/CLAUDE.md` **قديم ومضلل** — يحيل إلى مجلدات (`docs/`, `design-system/`, `claude-code/`) لم تعد موجودة. تجاهله واعتمد هذا الملف.

---

## 8) حلقة العمل

```
افتح مهمة من TASKS.md  →  اعمل على فرع مستقل  →  typecheck + test + build
   →  ادفع الفرع  →  مراجعة معمارية  →  CI أخضر  →  دمج في main
```

- **فرع لكل مهمة**، لا عمل مباشر على `main`.
- رسالة الالتزام تشرح **لماذا**، لا ماذا فقط.
- إن اكتشفت مشكلة خارج نطاق مهمتك: **وثّقها ولا توسّع النطاق** من تلقاء نفسك.
