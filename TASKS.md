# TASKS.md — قائمة المهام المرتّبة بالأولوية

> اقرأ `AGENTS.md` أولاً. كل مهمة هنا محددة النطاق عمداً — **نفّذ مهمة واحدة على فرع واحد**، ولا توسّع النطاق.
> آخر تحديث: 2026-09-03 — مستمدة من الفجوات الموثّقة في `SPRUVEX_CURRENT_STATUS.md` و`PILOT_LAUNCH_PLAN.md`.

**دليل الرموز**: 🔴 حاجز إطلاق · 🟡 مهم · 🟢 تحسين | 🔒 يلمس منطقة خطر (§4 في AGENTS.md — يحتاج مراجعة قبل التنفيذ)

---

## 🔴 حواجز الإطلاق التجريبي

### T-01 — استعادة كلمة مرور مدير الشركة 🔒
**المشكلة**: مدير الشركة يستطيع إعادة تعيين كلمات مرور موظفيه (`PUT /api/auth/users/:id`)، لكن **لا أحد يستطيع إعادة تعيين كلمة مروره هو**. لا "نسيت كلمة المرور"، ولا مسار لمشرف المنصة. الحل الحالي: تعديل `password_hash` يدوياً في قاعدة البيانات.
**المطلوب**: مسار استعادة حقيقي عبر البريد (رمز محدود الصلاحية، مرة واحدة، مُخزَّن مُجزَّأً) + مسار احتياطي لمشرف المنصة.
**شروط القبول**: الرمز ينتهي خلال مدة قصيرة · لا يُعاد استخدامه · محدِّد معدل صارم على نقطة الطلب · لا يكشف وجود البريد من عدمه · اختبارات تغطي الثلاثة.
**لماذا 🔒**: يلمس المصادقة مباشرة.

### T-02 — توحيد عدّاد المخزون 🔒
**المشكلة**: مسار الدفع اليدوي في الواجهة يحدّث `products.stock` مباشرة، بينما التحويلات والتسويات تمر عبر محرك المستودعات. تاجر بمستودع واحد لن يلاحظ، لكن من يستخدم التحويلات سيرى رقمين مختلفين.
**المطلوب**: توحيد كل خصم/إضافة مخزون عبر محرك المستودعات وحده.
**شروط القبول**: اختبار تكامل يثبت تطابق الرقمين بعد بيع + تحويل + تسوية · لا كسر لأي مسار بيع قائم.

### T-03 — تفعيل Sentry
**المشكلة**: لا خدمة تتبّع أخطاء مفعّلة. نقطة الدمج جاهزة ومحددة: `core/errors/errorHandler.ts`.
**المطلوب**: دمج Sentry خلف متغير `SENTRY_DSN` — إن كان فارغاً لا يتغير أي سلوك.
**شروط القبول**: خطأ 500 مُصطنع يظهر في Sentry · لا تُرسَل أي بيانات حساسة (كلمات مرور، توكنات، بيانات عملاء) · تشغيل بلا `SENTRY_DSN` يعمل طبيعياً.

---

## 🟡 مهم

### T-04 — `branchId` في الـJWT 🔒
**المشكلة**: مسار الدخول لا يحمل `branchId`، فتقارير أداء الفروع تعرض كل المبيعات تحت "غير مصنّف".
**المطلوب**: تضمين `branchId` في التوكن وتعبئة `sales.branchId` من كل مسارات البيع.
**شروط القبول**: تقرير الفروع يعرض أرقاماً حقيقية · التوكنات القديمة لا تنكسر (تعامل مع غياب الحقل) · لا يُقرأ `branchId` من العميل أبداً.

### T-05 — استيراد منتجات من ملف
**المشكلة**: `POST /api/products/bulk` موجود لكن بلا واجهة — إدخال منتجات أول تاجر يتم يدوياً.
**المطلوب**: واجهة رفع CSV/Excel: معاينة → تحقق → استيراد، مع تقرير أخطاء لكل صف.
**شروط القبول**: ملف 500 منتج يُستورد · صف خاطئ لا يُفشل الباقي · دعم العربية والإنجليزية في الأعمدة.

### T-06 — اختبار وضع عدم الاتصال (Offline) ميدانياً
**المشكلة**: يوجد أساس offline queue/sync في الكود لكنه **لم يُختبر end-to-end** إطلاقاً.
**المطلوب**: اختبارات آلية تحاكي انقطاع الشبكة أثناء بيع، ثم استعادتها.
**شروط القبول**: لا فقدان بيع · لا بيع مكرر عند إعادة الاتصال · توثيق صادق لما يعمل وما لا يعمل.

### T-07 — ترحيل المسارات القديمة إلى بنية الموديولات
**المشكلة**: `artifacts/api-server/src/routes/` يحوي مسارات قديمة (منتجات، تصنيفات، صيانة، جلسات نقدية...) بلا فصل service/repository.
**المطلوب**: ترحيل **مسار واحد فقط لكل مهمة/فرع** إلى نمط `modules/`.
**شروط القبول**: سلوك مطابق تماماً · نفس عقد الـAPI · اختبارات قبل وبعد.
**ملاحظة**: ابدأ بالأصغر. لا ترحّل أكثر من مسار واحد في فرع واحد.

---

## 🟢 تحسينات

### T-08 — تغطية اختبارات أوسع
الحالي: 33 اختبار وحدة + 8 تكامل. الفجوات الأوضح: الصلاحيات (RBAC)، حدود الاشتراك، سلسلة ZATCA، حسابات الضريبة.

### T-09 — قياس الأداء
لا قياسات Lighthouse ولا قياس زمن استجابة POS محفوظة. الهدف المعلن: استجابة POS < 300ms.

### T-10 — تنظيف التوثيق القديم
`spruvex-app/CLAUDE.md` يحيل لمجلدات غير موجودة (`docs/`, `design-system/`, `claude-code/`) — إما يُحدَّث أو يُحذف.

---

## ⏸️ مؤجَّل عمداً — لا تعمل عليها

| البند | سبب التأجيل |
|---|---|
| تفعيل تكاملات سلة/زد/Shopify فعلياً | تحتاج حسابات مطوّر حقيقية |
| تفعيل تابي/تمارا/Moyasar فعلياً | تحتاج حسابات تاجر حقيقية |
| فواتير PDF/A-3 | تخص المرحلة الثانية من ZATCA — الطباعة الحالية تغطي الالتزام الحالي |
| المواقع التسويقية | انتقلت لمستودع `spruvex-s-erp-r` |
| قرارات الاستضافة والنطاق | قرار مالك المشروع |
| واجهة لوحات BI ولوحة الذكاء الاصطناعي | الأساس (API) مكتمل — الواجهة بانتظار أولوية |
