# TASKS.md — قائمة المهام المرتّبة بالأولوية

> اقرأ `AGENTS.md` أولاً. كل مهمة هنا محددة النطاق عمداً — **نفّذ مهمة واحدة على فرع واحد**، ولا توسّع النطاق.
> آخر تحديث: 2026-09-06 — خطة مرحلية لاستكمال مشروع ERP (قرار معماري، انظر §0 أدناه). كل ما قبلها مستمد من `SPRUVEX_CURRENT_STATUS.md` و`PILOT_LAUNCH_PLAN.md`.

## 🚀 §0 — خطة استكمال ERP (4 مراحل، بالترتيب — لا تقفز مرحلة)

المرحلة 1 نشِطة الآن. لا تبدأ مرحلة تالية قبل إغلاق التي قبلها في هذا الملف.

1. **المرحلة 1 (الآن)** — 🔴 إصلاح تصادم مسارات المبيعات + استكمال T-07 → التفاصيل في T-07 أدناه.
2. **المرحلة 2** — 🟡 T-12 إقرار ضريبي VAT (تقرير مجمّع للتصدير، **ليس** تكاملاً مباشراً مع بوابة الهيئة — النطاق محدد أدناه).
3. **المرحلة 3** — 🟡 T-16 وحدة الموارد البشرية HR (تأسيس: موظفون/أقسام/حضور فقط — لا رواتب في هذه المرحلة).
4. **المرحلة 4** — 🔒 ZATCA Phase 2 الفعلي — **معلّقة على إجراء إداري خارجي من صاحب المشروع** (فتح حساب Fatoora والحصول على CSID)، وليست مهمة برمجية قابلة للبدء الآن.

**دليل الرموز**: 🔴 حاجز إطلاق · 🟡 مهم · 🟢 تحسين | 🔒 يلمس منطقة خطر (§4 في AGENTS.md — يحتاج مراجعة قبل التنفيذ)

---

## 🔴 حواجز الإطلاق التجريبي

### T-01 — استعادة كلمة مرور مدير الشركة ✅ منجزة (PR #10، دُمجت 2026-09-03)
مسار OTP بالبريد كان موجوداً أصلاً في القاعدة؛ أُضيف المسار الاحتياطي `POST /api/platform/users/:id/reset-password` خلف `requirePlatformAdmin` الموجود مسبقاً، بلا فحص اشتراك (مقصود — تاجر معلَّق يحتاج استعادة أيضاً)، مع تدقيق (audit). راجعت الكود سطراً سطراً + قرأت اختبار التكامل. **لم أتمكن من تشغيل اختبار التكامل حياً بنفسي** في هذه الجلسة بسبب بطء شبكة هذه البيئة تحديداً نحو Neon (مهلات زمنية حتى مع 60 ثانية على عمليات إدراج بسيطة) — قيد بيئي مؤكَّد أثر على كل ملفات التكامل بلا استثناء، لا خطأ في الكود.

### T-02 — توحيد عدّاد المخزون ✅ منجزة (PR #11، دُمجت 2026-09-03)
المحرك الموحّد كان موجوداً أصلاً؛ أُضيف اختبار تكامل يثبت تطابق `products.stock` مع `SUM(stock_table.quantity)` بعد بيع+تحويل+تسوية. لا كود إنتاج تغيّر. نفس قيد التحقق الحي المذكور في T-01 ينطبق هنا.

### T-11 — سد فجوة فحص الاشتراك في مسارَي ZATCA والبيع الحديث ✅ منجزة (PR #4، دُمجت 2026-09-03)
`requireActiveSubscription()` أُضيفت لكلا الملفين، داخل راوتر كل موديول لا الراوتر التجميعي — تحقّقتُ منه بنفسي على نسخة محلية منفصلة (typecheck + 33/33 اختبار وحدة + قراءة كاملة لملفي اختبار التكامل الجديدين) قبل الدمج. **ملاحظة دقة صغيرة غير حاجزة**: نصف اختبار "C4" الخاص بتأكد عدم تأثر تسجيل الدخول استخدم إعادة تنفيذ مصغّرة لمنطق `/api/auth/login` لا الراوتر الحقيقي نفسه (منطق الدخول لم يُلمس أصلاً في هذا الإصلاح، فالمخاطرة منخفضة) — إعادة تحقق حية لاحقاً ضد الراوتر الفعلي تبقى ممكنة كخطوة تحسين غير عاجلة.

### T-12 — إقرار ضريبي (VAT Return) 🟡 المرحلة 2 — النطاق محدَّد الآن (قرار معماري 2026-09-06)، **لا تبدأ قبل إغلاق المرحلة 1 (T-07)**
**المشكلة**: النظام يُصدر فواتير ضريبية فردية فقط عبر `modules/zatca` — لا يوجد مسار أو جدول لإقرار ضريبي مجمّع (شهري/ربع سنوي).

**النطاق المعتمد (تقرير مجمّع فقط، بلا تكامل مباشر مع بوابة الهيئة)** — هيئة الزكاة والضريبة والجمارك (ZATCA) لا توفر أي API عام لتقديم الإقرار آلياً؛ التقديم الفعلي يبقى يدوياً عبر بوابتها. المطلوب برمجياً:
1. جدول `vat_returns` (companyId، periodStart، periodEnd، totalSalesExVat، totalOutputVat، totalPurchasesExVat، totalInputVat، netVatDue، status: draft/finalized، finalizedAt، finalizedBy) — مع سجل تدقيق عند finalize (لا حذف/تعديل بعد finalized، فقط تصحيح بقيد عكسي إن لزم لاحقاً).
2. خدمة تجميع: تسحب `totalOutputVat` من فواتير `modules/zatca` الصادرة ضمن الفترة. **راجع أولاً** هل توجد بيانات VAT مدخلة فعلاً على المشتريات في `modules/purchases`/`modules/expenses` — إن لم توجد حقول ضريبة مدخلة، اجعل `totalInputVat` صفراً/يدوي الإدخال في هذه المرحلة ووثّق ذلك صراحة (لا تخترع بيانات).
3. مسار API: `GET /api/accounting/vat-returns` (قائمة)، `POST /api/accounting/vat-returns/generate` (يُنشئ مسودة من فترة)، `POST /api/accounting/vat-returns/:id/finalize`. خلف `requireAuth + enforceTenantIsolation + requireActiveSubscription() + requirePermission` مناسبة (أنشئ صلاحية جديدة إن لزم، لا تُعِد استخدام صلاحية غير دقيقة الدلالة).
4. تصدير: CSV على الأقل يطابق حقول نموذج إقرار ضريبة القيمة المضافة القياسي (لا PDF إلزامي في هذه المرحلة).
5. **ممنوع في هذه المرحلة**: أي اتصال فعلي أو محاكاة اتصال ببوابة ZATCA للإقرار (لا يوجد API رسمي أصلاً) — هذا تقرير داخلي مساعد للتقديم اليدوي فقط، ووثّق هذا القيد بوضوح في أي مستند تسليم.

### T-13 — توضيح حالة اعتماد ZATCA الفعلية ✅ منجزة فعلياً (PR #15، دُمجت 2026-09-03)
تحذير صريح أُضيف لـ`PILOT_LAUNCH_PLAN.md` (بعد §5أ عن ZATCA): يذكر HMAC-STUB بدل ECDSA/CSID، وأن `submitToZATCA` لا يتصل بخادم حقيقي، وأن onboarding فعلي (CSR+OTP+CSID) مطلوب قبل أول عميل حقيقي. اختبرت الدمج محلياً قبل الدفع (الفرع تفرّع من قاعدة قديمة، الدمج نظيف ويقتصر على الإضافة المقصودة فقط) وتحقّقت من وجود النص الفعلي بعد الدمج. **يصحح** المحاولة الأولى (`docs/T-13-pilot-launch-update`، PR #6) التي وثّقت شيئاً آخر خطأً باسم T-13.

### T-03 — تفعيل Sentry ✅ منجزة (PR #3، دُمجت 2026-09-03)
دُمج `errorHandler.ts` بتتبّع Sentry خلف `SENTRY_DSN`، تحقّقتُ منه بنفسي على نسخة محلية منفصلة (تثبيت + typecheck + 33/33 اختبار + build) قبل الدمج — لا اعتماد على التقرير وحده. **الباقي**: إعادة تحقق حية ضد DSN حقيقي من sentry.io عند توفره لاحقاً (ليست حاجزاً).

---

## 🟡 مهم

### T-04 — `branchId` في الـJWT ✅ منجزة (PR #12، دُمجت 2026-09-03)
تبيّن أن `branchId` كان مطبَّقاً بالفعل بالكامل عبر السلسلة (توقيع التوكن، استخراجه في auth middleware، تعبئة sales.branchId) قبل هذا الفرع — أكّدته بنفسي عبر grep مباشر في الكود. مساهمة الفرع الفعلية: اختبار تكامل فقط يثبت السلوك ويؤكد أن التوكنات القديمة بلا الحقل لا تنكسر (NULL بدل خطأ).

### T-05 — استيراد منتجات من ملف ✅ منجزة (PR #9، دُمجت 2026-09-03)
واجهة 3 خطوات (رفع→معاينة→استيراد) تستخدم `POST /products/bulk` الموجود مسبقاً — لا سطح API جديد. راجعت الكود: لا `eval`، لا `dangerouslySetInnerHTML`.

### T-06 — اختبار وضع عدم الاتصال (Offline) ميدانياً ✅ منجزة (PR #13، دُمجت 2026-09-03)
4 اختبارات تكامل ضد طبقة خدمة offline-queue الحقيقية + `docs/T06_OFFLINE_TEST_FINDINGS.md` يوثّق بصدق ما لم يُختبَر (لا محاكاة انقطاع شبكة حقيقي — لا يوجد عميل offline فعلي في المستودع، لا اختبار مصالحة سحب، فجوة ذرية موثّقة بين commit البيع وتحديث حالة الطابور).

### T-07 — ترحيل المسارات القديمة إلى بنية الموديولات 🔴 نشِطة (المرحلة 1) — `/brands` منجزة (PR #14)، الباقي مطلوب الآن
راجعت الـdiff الكامل لـ`/brands`: إزالة نظيفة للمسار القديم من `routes/index.ts`، تركيب واحد في `app.ts`، الموديول الجديد يطبّق `requireAuth + enforceTenantIsolation + requireActiveSubscription()` بنفس نمط T-11 (لا تسريب ممكن).

**✅ تصادم مسارات المبيعات — مُغلَق (2026-09-11)**: أوبن كود حقّق حياً (شغّل السيرفر فعلاً، لا قراءة كود فقط) وأكّد: `modules/sales/routes/sales.ts` (القديم) هو المسار الحي فعلياً — يدعم مسودات، دفعات جزئية/مقسّمة، ربط جلسة الصندوق، وقيود محاسبية كاملة عبر `postSaleEntry`. `modules/pos/routes/sales.routes.ts` (الموديولي) كان كوداً ميتاً فعلاً — لا يدعم إلا بيعاً مكتملاً بدفع كامل، بلا قيود محاسبية. حذف الملف الميت (`sales.routes.ts` + `saleController.ts`) ونظّف `app.ts`. **لاحظ أوبن كود نقطة مهمة لم أطلبها صراحة**: خدمة `modules/pos/services/saleService.ts` نفسها (وليس الراوتر) **ليست ميتة** — تُستخدم مباشرة (استيراد مباشر، لا عبر HTTP) من `ecommerceService.ts` و`offlineQueueProcessor.ts`، فبقيت كما هي.

دمجتُ هذا الفرع بنفسي (cherry-pick يدوي + حل تعارض) بعد أن اكتشفتُ أن أوبن كود تفرّع من `main` القديم (راكد منذ 15 أغسطس) بدل هذا الفرع الفعلي — انظر ملاحظة العملية أسفل هذا القسم. عند الدمج وجدتُ ملف اختبار تكامل يتيماً (`modules/pos/routes/sales.integration.test.ts`، من T-11/PR #4) يستورد الملف المحذوف مباشرة — **كان سيكسر `typecheck` فعلياً** رغم أن رسالة الالتزام ادّعت "typecheck ✅" (تناقض حقيقي، على الأرجح لأن فرعهم لم يحتوِ هذا الملف أصلاً بسبب مشكلة القاعدة). أصلحته بنفسي: نقلته إلى `modules/sales/routes/` وأعدت كتابته ليختبر المسار الحي الفعلي (`modules/sales/routes/sales.ts`) مع تطبيق نفس سلسلة الحماية الحقيقية في الإنتاج (`requireAuth + requireActiveSubscription` عند نقطة التركيب، وليس داخل الراوتر نفسه — لأن هذا الراوتر القديم لا يحمي نفسه داخلياً، الحماية مطبّقة في `routes/index.ts` فقط). صححت أيضاً حمولة الاختبار (كانت تفترض شكل payload خاطئاً بالمسار الميت: `payments[].paymentMethodId` بينما الخدمة الحية تتطلب `payments[].methodName`، واستخدمت بدلاً منه المسار البسيط `paymentMethodId + amountPaid + unitPrice`). تحقّقت: `typecheck` نظيف الآن (api-server + pos-system)، 101/101 اختبار وحدة ناجح. الاختبار الحي ضد Neon واجه نفس قيد الشبكة الموثّق سابقاً (مهلة hook 10 ثانية رغم تمرير `--hookTimeout 60000`) — نفس القيد البيئي المسجَّل في T-01/T-02، لا علاقة بالكود.

**⚠️ ملاحظة عملية مهمة لأوبن كود**: هذا الفرع (`fix/sales-route-conflict`) تفرّع من `main`، وهو **راكد منذ 15 أغسطس ولا يحتوي أياً من عمل T-01 حتى T-16 ولا `AGENTS.md`/`TASKS.md` المحدَّثين**. الفرع الصحيح للعمل عليه دائماً هو `claude/project-analysis-status-v7bh07` (كل الدمج يحدث إليه، وهو المرجع الوحيد الحالي لحالة المشروع). **قبل أي فرع جديد: تأكد أنك تفرّعت من هذا الفرع تحديداً، لا من `main`.**

**بعد ذلك — استكمال الترحيل (مسار واحد لكل فرع، بنفس نمط `/brands`)، بهذا الترتيب المقترح** (الأبسط أولاً):
`units` → `order-types` → `device-models` → `product-attributes` → `payment-methods` → `warehouses` → `barcode-search` → `categories` → `settings` → `onboarding` → `import` / `export` → `audit-log` → `invoice-templates` → `installment-plans` / `installment-sales` → `products` (⚠️ لاحظ وجود `modules/products/` و`modules/inventory/` مجلدات جزئية موجودة أصلاً — راجعها أولاً قبل إنشاء موديول جديد، قد يكون العمل بدأ ولم يُكتمل توصيله) → `cash-sessions` → `repairs` / `repair-parts` → `reports` (الأعقد، آخر واحد).

لكل فرع: نفس معايير `/brands` تماماً — إزالة نظيفة من `routes/index.ts`، تركيب مباشر في `app.ts`، `requireAuth + enforceTenantIsolation + requireActiveSubscription()` داخل راوتر الموديول نفسه لا التجميعي.

---

## 🟢 تحسينات

### T-08 — تغطية اختبارات أوسع ✅ منجزة (PR #7، دُمجت 2026-09-03)
101 اختبار وحدة بدل 33 (+68: صلاحيات RBAC، حدود الاشتراك، سلسلة ZATCA). تحقّقتُ بنفسي بنفس الرقم تماماً (101/101) على الشجرة المدموجة. حسابات الضريبة لم تُغطَّ — مضمّنة داخل خدمات DB بلا helper نقي قابل للعزل، موثّق بصدق في رسالة الالتزام.

### T-09 — قياس الأداء ✅ منجزة جزئياً (PR #8، دُمجت 2026-09-03)
قياس فعلي لـ`createSale`: p50 ~2470ms عبر Neon سحابية — الزمن مهيمن عليه بزمن الشبكة (5-8 استعلامات متسلسلة). الهدف <300ms **غير قابل للتحقق سحابياً** بمعزل عن مكان استضافة API. Lighthouse لم يُنفَّذ — يحتاج متصفحاً ونشراً فعلياً حقيقياً.

### T-14 — جسر API لبيانات المنصة إلى لوحة تسويق موحّدة (مؤجَّل، يحتاج تحديد نطاق أولاً) 🟡
**السياق**: مطلوب لوحة تحكم تسويقية واحدة (تُبنى حالياً كواجهة داخل `spruvex-s` في مستودع `spruvex-s-erp-r`) تعرض بيانات حية عن الشركات المشتركة في ERP وSpruVex R. **لا يجوز** الوصول المباشر لقاعدتَي بيانات المنتجين من هذه اللوحة — يخالف قاعدة العزل الكامل بين المنتجين. المطلوب بدلاً من ذلك: نقطة API للقراءة فقط (مثلاً `GET /api/platform/marketing-summary`) خلف `requirePlatformAdmin` الموجود أصلاً، تُعيد ملخصاً مجمّعاً (عدد شركات، حالات اشتراك، إلخ) بلا بيانات حساسة تفصيلية.
**⛔ لا تبدأ التنفيذ قبل تحديد الحقول المطلوبة تحديداً من صاحب المشروع.**

### T-15 — نظام تسويق بالعمولة حقيقي (مؤجَّل، يحتاج تحديد نطاق أولاً) 🔒
**السياق**: زر "كن شريكاً" سيُبنى الآن في المواقع الثلاثة كنموذج جمع اهتمام بسيط (بلا تتبّع) عبر `spruvex-s`. نظام العمولة الحقيقي (روابط إحالة فريدة، تتبّع نقرات وتحويلات، نسبة عمولة، لوحة أرباح للمسوّق، آلية صرف) يلمس المال مباشرة — يحتاج تصميم منتج كامل (أين تُخزَّن الإحالات؟ كيف تُربط ببيع فعلي في ERP أو R؟ من يعتمد الصرف؟) قبل أي سطر كود.
**⛔ لا تبدأ التنفيذ قبل جلسة تصميم منتج منفصلة.**

### T-16 — تأسيس وحدة الموارد البشرية (HR) 🟡 المرحلة 3 — **لا تبدأ قبل إغلاق المرحلتين 1 و2**
**المشكلة**: لا توجد وحدة HR إطلاقاً حالياً (تحقّق 2026-09-06) — لا موظفين كوجدول منفصل عن `users`، لا أقسام، لا حضور/انصراف.

**النطاق المعتمد لهذه المرحلة (تأسيس فقط — لا رواتب، لا إجازات، لا عقود)**:
1. `modules/hr` جديد بنفس البنية القياسية (routes/services/repositories/validators/controllers).
2. جدول `departments` (companyId، name، managerId اختياري).
3. جدول `employees` — منفصل عن `users` عمداً (ليس كل موظف بالضرورة له حساب دخول للنظام؛ العكس صحيح غالباً — كل `user` مرتبط بموظف واحد اختيارياً عبر `employeeId` nullable على `users` أو العكس، اختر الاتجاه الأقل اضطراباً للمخطط الحالي وبرر القرار في رسالة الالتزام). حقول أساسية: name، nationalId، departmentId، position، hireDate، status (active/terminated)، branchId.
4. جدول `attendance` بسيط: employeeId، clockIn، clockOut، source (manual/kiosk) — بلا حساب رواتب أو ساعات إضافية في هذه المرحلة.
5. مسارات CRUD قياسية خلف `requireAuth + enforceTenantIsolation + requireActiveSubscription() + requirePermission` (صلاحيات HR جديدة: `HR_VIEW`, `HR_MANAGE`).
6. **ممنوع صراحة في هذه المرحلة**: الرواتب/البدلات، حساب الإجازات ورصيدها، أي حساب مالي يمسّ `modules/accounting`. هذه لمرحلة لاحقة بعد مراجعة نطاق منفصلة معي.
**نفّذها على أكثر من فرع إذا لزم** (مثلاً: فرع للجداول + الموديول الأساسي، فرع منفصل للحضور) بدل فرع ضخم واحد.

### T-10 — تنظيف التوثيق القديم ✅ منجزة (PR #5، دُمجت 2026-09-03)
`spruvex-app/CLAUDE.md` حُذف بالكامل — `AGENTS.md` هو المرجع الآن.

---

## ⏸️ مؤجَّل عمداً — لا تعمل عليها

| البند | سبب التأجيل |
|---|---|
| تفعيل تكاملات سلة/زد/Shopify فعلياً | تحتاج حسابات مطوّر حقيقية |
| تفعيل تابي/تمارا/Moyasar فعلياً | تحتاج حسابات تاجر حقيقية |
| فواتير PDF/A-3 | تخص المرحلة الثانية من ZATCA — الطباعة الحالية تغطي الالتزام الحالي |
| المواقع التسويقية | انتقلت لمستودع `spruvex-s-erp-r` |
| قرارات الاستضافة والنطاق | قرار مالك المشروع |
| واجهة لوحات BI ولوحة الذكاء الاصطناعي | الأساس (API) مكتمل — الواجهة بانتظار أولوية |
| **المرحلة 4: ZATCA Phase 2 الفعلي** (توقيع ECDSA حقيقي + CSID + اتصال فعلي بخادم الهيئة، بدل الـHMAC-STUB الحالي الموثَّق في T-13) | يتطلب أولاً إجراءً إدارياً خارجياً من صاحب المشروع (فتح حساب في بوابة Fatoora، إصدار CSR، الحصول على CSID) — **ليست مهمة برمجية قابلة للجدولة الآن**. عند إتمام هذا الإجراء إدارياً، تُفتح مهمة برمجية جديدة هنا بنطاق دقيق. |
