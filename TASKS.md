# TASKS.md — قائمة المهام المرتّبة بالأولوية

> اقرأ `AGENTS.md` أولاً. كل مهمة هنا محددة النطاق عمداً — **نفّذ مهمة واحدة على فرع واحد**، ولا توسّع النطاق.
> آخر تحديث: 2026-09-03 — مستمدة من الفجوات الموثّقة في `SPRUVEX_CURRENT_STATUS.md` و`PILOT_LAUNCH_PLAN.md`.

**دليل الرموز**: 🔴 حاجز إطلاق · 🟡 مهم · 🟢 تحسين | 🔒 يلمس منطقة خطر (§4 في AGENTS.md — يحتاج مراجعة قبل التنفيذ)

---

## 🔴 حواجز الإطلاق التجريبي

### T-01 — استعادة كلمة مرور مدير الشركة 🔒
**المشكلة**: مدير الشركة يستطيع إعادة تعيين كلمات مرور موظفيه (`PUT /api/auth/users/:id`)، لكن **لا أحد يستطيع إعادة تعيين كلمة مروره هو**. لا "نسيت كلمة المرور"، ولا مسار لمشرف المنصة. الحل الحالي: تعديل `password_hash` يدوياً في قاعدة البيانات.
**المطلوب**: مسار استعادة حقيقي عبر البريد (رمز محدود الصلاحية، مرة واحدة، مُخزَّن مُجزَّأً) + مسار احتياطي لمشرف المنصة.
**شروط القبول**: الرمز ينتهي خلال مدة قصيرة · لا يُعاد استخدامه · محدِّد معدل صارم على نقطة الطلب · لا يكشف وجود البريد من عدمه · اختبارات تغطي الثلاثة.
**لماذا 🔒**: يلمس المصادقة مباشرة.

### T-02 — توحيد عدّاد المخزون 🔒
**المشكلة**: مسار الدفع اليدوي في الواجهة يحدّث `products.stock` مباشرة، بينما التحويلات والتسويات تمر عبر محرك المستودعات. تاجر بمستودع واحد لن يلاحظ، لكن من يستخدم التحويلات سيرى رقمين مختلفين.
**المطلوب**: توحيد كل خصم/إضافة مخزون عبر محرك المستودعات وحده.
**شروط القبول**: اختبار تكامل يثبت تطابق الرقمين بعد بيع + تحويل + تسوية · لا كسر لأي مسار بيع قائم.

### T-11 — سد فجوة فحص الاشتراك في مسارَي ZATCA والبيع الحديث ✅ منجزة (PR #4، دُمجت 2026-09-03)
`requireActiveSubscription()` أُضيفت لكلا الملفين، داخل راوتر كل موديول لا الراوتر التجميعي — تحقّقتُ منه بنفسي على نسخة محلية منفصلة (typecheck + 33/33 اختبار وحدة + قراءة كاملة لملفي اختبار التكامل الجديدين) قبل الدمج. **ملاحظة دقة صغيرة غير حاجزة**: نصف اختبار "C4" الخاص بتأكد عدم تأثر تسجيل الدخول استخدم إعادة تنفيذ مصغّرة لمنطق `/api/auth/login` لا الراوتر الحقيقي نفسه (منطق الدخول لم يُلمس أصلاً في هذا الإصلاح، فالمخاطرة منخفضة) — إعادة تحقق حية لاحقاً ضد الراوتر الفعلي تبقى ممكنة كخطوة تحسين غير عاجلة.

### T-12 — إقرار ضريبي (VAT Return) 🟡
**المشكلة**: النظام يُصدر فواتير ضريبية فردية فقط عبر `modules/zatca` — **لا يوجد أي مسار أو جدول لإقرار ضريبي مجمّع** (شهري/ربع سنوي) قابل للتقديم لهيئة الزكاة. غير موجود إطلاقاً حالياً (تحقّق 2026-09-03).
**المطلوب**: تحديد نطاق دقيق مع صاحب المشروع أولاً — هذا قرار منتج قبل أن يكون مهمة برمجية (هل هو تقرير قابل للتصدير فقط، أم تكامل فعلي مع بوابة الإقرار؟).
**⛔ لا تبدأ التنفيذ قبل تأكيد النطاق من صاحب المشروع.**

### T-13 — توضيح حالة اعتماد ZATCA الفعلية 🟡
**المشكلة**: المرحلة الأولى (التوليد: hash chain، QR TLV/Base64، XML UBL) حقيقية وتعمل. **المرحلة الثانية (Integration/Clearance) غير موجودة فعلياً** — `signInvoiceHash` في `signingService.ts` توقيع HMAC مؤقت (`algorithm: "HMAC-SHA256-STUB"`) وليس ECDSA بشهادة CSID حقيقية، و`submitToZATCA` في `zatcaService.ts` **لا يتصل بأي خادم لهيئة الزكاة إطلاقاً** — يبني شكل الطلب، يسجّله، ويحاكي استجابة ناجحة دائماً. موثّق بصدق في تعليقات الكود نفسها لكن غير مذكور كحاجز صريح في `PILOT_LAUNCH_PLAN.md`.
**الأثر**: النظام **غير معتمد فعلياً من هيئة الزكاة حالياً** — أي إطلاق تجاري حقيقي يحتاج إجراء onboarding فعلي (CSR + OTP + شهادة CSID) قبل أول عميل حقيقي، وهذه ليست مهمة كود بل إجراء إداري خارجي مع الهيئة.
**المطلوب من OpenCode**: لا شيء برمجي الآن — فقط إضافة هذا التحذير صراحة إلى `PILOT_LAUNCH_PLAN.md` كحاجز معروف. تنفيذ التوقيع الحقيقي (استبدال جسم `signInvoiceHash` و`submitToZATCA`) يبقى مؤجَّلاً حتى تتوفر شهادة CSID حقيقية.

### T-03 — تفعيل Sentry ✅ منجزة (PR #3، دُمجت 2026-09-03)
دُمج `errorHandler.ts` بتتبّع Sentry خلف `SENTRY_DSN`، تحقّقتُ منه بنفسي على نسخة محلية منفصلة (تثبيت + typecheck + 33/33 اختبار + build) قبل الدمج — لا اعتماد على التقرير وحده. **الباقي**: إعادة تحقق حية ضد DSN حقيقي من sentry.io عند توفره لاحقاً (ليست حاجزاً).

---

## 🟡 مهم

### T-04 — `branchId` في الـJWT 🔒
**المشكلة**: مسار الدخول لا يحمل `branchId`، فتقارير أداء الفروع تعرض كل المبيعات تحت "غير مصنّف".
**المطلوب**: تضمين `branchId` في التوكن وتعبئة `sales.branchId` من كل مسارات البيع.
**شروط القبول**: تقرير الفروع يعرض أرقاماً حقيقية · التوكنات القديمة لا تنكسر (تعامل مع غياب الحقل) · لا يُقرأ `branchId` من العميل أبداً.

### T-05 — استيراد منتجات من ملف
**المشكلة**: `POST /api/products/bulk` موجود لكن بلا واجهة — إدخال منتجات أول تاجر يتم يدوياً.
**المطلوب**: واجهة رفع CSV/Excel: معاينة → تحقق → استيراد، مع تقرير أخطاء لكل صف.
**شروط القبول**: ملف 500 منتج يُستورد · صف خاطئ لا يُفشل الباقي · دعم العربية والإنجليزية في الأعمدة.

### T-06 — اختبار وضع عدم الاتصال (Offline) ميدانياً
**المشكلة**: يوجد أساس offline queue/sync في الكود لكنه **لم يُختبر end-to-end** إطلاقاً.
**المطلوب**: اختبارات آلية تحاكي انقطاع الشبكة أثناء بيع، ثم استعادتها.
**شروط القبول**: لا فقدان بيع · لا بيع مكرر عند إعادة الاتصال · توثيق صادق لما يعمل وما لا يعمل.

### T-07 — ترحيل المسارات القديمة إلى بنية الموديولات
**المشكلة**: `artifacts/api-server/src/routes/` يحوي مسارات قديمة (منتجات، تصنيفات، صيانة، جلسات نقدية...) بلا فصل service/repository.
**المطلوب**: ترحيل **مسار واحد فقط لكل مهمة/فرع** إلى نمط `modules/`.
**شروط القبول**: سلوك مطابق تماماً · نفس عقد الـAPI · اختبارات قبل وبعد.
**ملاحظة**: ابدأ بالأصغر. لا ترحّل أكثر من مسار واحد في فرع واحد.

---

## 🟢 تحسينات

### T-08 — تغطية اختبارات أوسع
الحالي: 33 اختبار وحدة + 8 تكامل. الفجوات الأوضح: الصلاحيات (RBAC)، حدود الاشتراك، سلسلة ZATCA، حسابات الضريبة.

### T-09 — قياس الأداء
لا قياسات Lighthouse ولا قياس زمن استجابة POS محفوظة. الهدف المعلن: استجابة POS < 300ms.

### T-14 — جسر API لبيانات المنصة إلى لوحة تسويق موحّدة (مؤجَّل، يحتاج تحديد نطاق أولاً) 🟡
**السياق**: مطلوب لوحة تحكم تسويقية واحدة (تُبنى حالياً كواجهة داخل `spruvex-s` في مستودع `spruvex-s-erp-r`) تعرض بيانات حية عن الشركات المشتركة في ERP وSpruVex R. **لا يجوز** الوصول المباشر لقاعدتَي بيانات المنتجين من هذه اللوحة — يخالف قاعدة العزل الكامل بين المنتجين. المطلوب بدلاً من ذلك: نقطة API للقراءة فقط (مثلاً `GET /api/platform/marketing-summary`) خلف `requirePlatformAdmin` الموجود أصلاً، تُعيد ملخصاً مجمّعاً (عدد شركات، حالات اشتراك، إلخ) بلا بيانات حساسة تفصيلية.
**⛔ لا تبدأ التنفيذ قبل تحديد الحقول المطلوبة تحديداً من صاحب المشروع.**

### T-15 — نظام تسويق بالعمولة حقيقي (مؤجَّل، يحتاج تحديد نطاق أولاً) 🔒
**السياق**: زر "كن شريكاً" سيُبنى الآن في المواقع الثلاثة كنموذج جمع اهتمام بسيط (بلا تتبّع) عبر `spruvex-s`. نظام العمولة الحقيقي (روابط إحالة فريدة، تتبّع نقرات وتحويلات، نسبة عمولة، لوحة أرباح للمسوّق، آلية صرف) يلمس المال مباشرة — يحتاج تصميم منتج كامل (أين تُخزَّن الإحالات؟ كيف تُربط ببيع فعلي في ERP أو R؟ من يعتمد الصرف؟) قبل أي سطر كود.
**⛔ لا تبدأ التنفيذ قبل جلسة تصميم منتج منفصلة.**

### T-10 — تنظيف التوثيق القديم
`spruvex-app/CLAUDE.md` يحيل لمجلدات غير موجودة (`docs/`, `design-system/`, `claude-code/`) — إما يُحدَّث أو يُحذف.

---

## ⏸️ مؤجَّل عمداً — لا تعمل عليها

| البند | سبب التأجيل |
|---|---|
| تفعيل تكاملات سلة/زد/Shopify فعلياً | تحتاج حسابات مطوّر حقيقية |
| تفعيل تابي/تمارا/Moyasar فعلياً | تحتاج حسابات تاجر حقيقية |
| فواتير PDF/A-3 | تخص المرحلة الثانية من ZATCA — الطباعة الحالية تغطي الالتزام الحالي |
| المواقع التسويقية | انتقلت لمستودع `spruvex-s-erp-r` |
| قرارات الاستضافة والنطاق | قرار مالك المشروع |
| واجهة لوحات BI ولوحة الذكاء الاصطناعي | الأساس (API) مكتمل — الواجهة بانتظار أولوية |
