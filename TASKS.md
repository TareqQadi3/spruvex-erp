# TASKS.md — قائمة المهام المرتّبة بالأولوية

> اقرأ `AGENTS.md` أولاً. كل مهمة هنا محددة النطاق عمداً — **نفّذ مهمة واحدة على فرع واحد**، ولا توسّع النطاق.
> آخر تحديث: 2026-09-11 — T-07، T-12، T-14، T-15، T-16 مُنجزة. **T-17 (تدقيق شامل + إصلاحات)**: الدفعة الأولى من الإصلاحات منجزة، والباقي backlog مُصنَّف بالأولوية (انظر أسفل). المرحلة 4 (ZATCA Phase 2) معلّقة على إجراء إداري خارجي من صاحب المشروع، وليست عملاً برمجياً. من هنا فصاعداً، Claude Code ينفّذ التعديلات مباشرة على `spruvex-app` بنفسه (لم يعد أوبن كود يُستخدم لهذا المشروع — قرار صاحب المشروع 2026-09-11)، مع نفس انضباط التحقق (typecheck + build + اختبارات + مراجعة سطر بسطر) قبل كل دفع. **قيد بيئي متكرر يستحق تتبعاً، وأهم عنصر متبقٍ فعلياً الآن**: هذه الجلسة لا تستطيع تطبيق migrations أو تشغيل خادم حي بموثوقية بسبب بطء/انقطاع الشبكة نحو Neon — كل الكود مُتحقَّق منه ثابتاً (typecheck/build/اختبارات) لكن **migrations 0026 (vat_returns)، 0027 (hr)، 0028 (affiliates) لم تُطبَّق فعلياً على أي قاعدة بعد** — يلزم تطبيقها (`pnpm -C lib/db run migrate`) من بيئة بشبكة طبيعية قبل استخدام T-12 أو T-16 أو T-15 فعلياً.

## 🚀 §0 — خطة استكمال ERP (4 مراحل، بالترتيب — لا تقفز مرحلة)

**المراحل 1، 2، 3 ✅ اكتملت جميعها برمجياً (2026-09-11)، وT-14/T-15 أُنجزا أيضاً بتفويض مباشر من صاحب المشروع**. لا مرحلة نشِطة حالياً — المرحلة 4 معلّقة خارجياً فقط (انظر أدناه).

1. **المرحلة 1** ✅ — إصلاح تصادم مسارات المبيعات + T-07 كاملاً (كل مسار قديم هُجِّر). انظر التفاصيل أسفل T-07.
2. **المرحلة 2** ✅ — T-12 إقرار ضريبي VAT مُنجزة برمجياً — **يبقى تطبيق migration 0026 على القاعدة الفعلية عند توفر بيئة بشبكة طبيعية**.
3. **المرحلة 3** ✅ — T-16 وحدة الموارد البشرية HR (تأسيس) مُنجزة برمجياً — **يبقى تطبيق migration 0027 على القاعدة الفعلية عند توفر بيئة بشبكة طبيعية**.
4. **المرحلة 4** — 🔒 ZATCA Phase 2 الفعلي — **معلّقة على إجراء إداري خارجي من صاحب المشروع** (فتح حساب Fatoora والحصول على CSID)، وليست مهمة برمجية قابلة للبدء الآن.

**إضافياً (خارج ترقيم المراحل الأربع، أُنجزا بتفويض مباشر 2026-09-11)**: T-14 (جسر بيانات تسويقية) وT-15 (نظام عمولة حقيقي مرحلة 1) — انظر تفاصيلهما أدناه، بما فيها قرار البنية التحتية لـT-15 (لا خادم/قاعدة بيانات لـspruvex-s بعد، فبُنيت داخل قاعدة ERP بدل ذلك، تُستدعى عبر API فقط من المنتجات الأخرى).

**دليل الرموز**: 🔴 حاجز إطلاق · 🟡 مهم · 🟢 تحسين | 🔒 يلمس منطقة خطر (§4 في AGENTS.md — يحتاج مراجعة قبل التنفيذ)

---

## 🔴 حواجز الإطلاق التجريبي

### T-01 — استعادة كلمة مرور مدير الشركة ✅ منجزة (PR #10، دُمجت 2026-09-03)
مسار OTP بالبريد كان موجوداً أصلاً في القاعدة؛ أُضيف المسار الاحتياطي `POST /api/platform/users/:id/reset-password` خلف `requirePlatformAdmin` الموجود مسبقاً، بلا فحص اشتراك (مقصود — تاجر معلَّق يحتاج استعادة أيضاً)، مع تدقيق (audit). راجعت الكود سطراً سطراً + قرأت اختبار التكامل. **لم أتمكن من تشغيل اختبار التكامل حياً بنفسي** في هذه الجلسة بسبب بطء شبكة هذه البيئة تحديداً نحو Neon (مهلات زمنية حتى مع 60 ثانية على عمليات إدراج بسيطة) — قيد بيئي مؤكَّد أثر على كل ملفات التكامل بلا استثناء، لا خطأ في الكود.

### T-02 — توحيد عدّاد المخزون ✅ منجزة (PR #11، دُمجت 2026-09-03)
المحرك الموحّد كان موجوداً أصلاً؛ أُضيف اختبار تكامل يثبت تطابق `products.stock` مع `SUM(stock_table.quantity)` بعد بيع+تحويل+تسوية. لا كود إنتاج تغيّر. نفس قيد التحقق الحي المذكور في T-01 ينطبق هنا.

### T-11 — سد فجوة فحص الاشتراك في مسارَي ZATCA والبيع الحديث ✅ منجزة (PR #4، دُمجت 2026-09-03)
`requireActiveSubscription()` أُضيفت لكلا الملفين، داخل راوتر كل موديول لا الراوتر التجميعي — تحقّقتُ منه بنفسي على نسخة محلية منفصلة (typecheck + 33/33 اختبار وحدة + قراءة كاملة لملفي اختبار التكامل الجديدين) قبل الدمج. **ملاحظة دقة صغيرة غير حاجزة**: نصف اختبار "C4" الخاص بتأكد عدم تأثر تسجيل الدخول استخدم إعادة تنفيذ مصغّرة لمنطق `/api/auth/login` لا الراوتر الحقيقي نفسه (منطق الدخول لم يُلمس أصلاً في هذا الإصلاح، فالمخاطرة منخفضة) — إعادة تحقق حية لاحقاً ضد الراوتر الفعلي تبقى ممكنة كخطوة تحسين غير عاجلة.

### T-12 — إقرار ضريبي (VAT Return) ✅ منجزة برمجياً (2026-09-11) — ⚠️ الترحيل (migration) لم يُطبَّق فعلياً على القاعدة بعد
بنيتُ الميزة كاملة: جدول `vat_returns` جديد (migration `0026_misty_mandrill.sql`، تحققت من محتواه — `CREATE TABLE` فقط، لا شيء آخر تغيّر)، مسارات `GET /api/accounting/vat-returns`، `POST /generate`، `GET /:id`، `GET /:id/export` (CSV)، `POST /:id/finalize` خلف `requirePermission(MANAGE_ACCOUNTING)`.

**مصادر البيانات حقيقية، لا مخترعة**: Output VAT من `invoicesTable.taxAmount` (فواتير ZATCA الصادرة فعلاً، غير المسودة) مع طرح credit_note وإضافة debit_note. Input VAT من `purchaseInvoicesTable.taxAmount` — تحققت أنه محسوب فعلياً من `settings.taxRate` عند كل عملية شراء/مرتجع شراء (`purchaseInvoiceService.ts`)، ليس حقلاً فارغاً افتراضياً كما توقعت التعليمة الأصلية أن يكون. فصلت حساب التجميع (`computeVatTotals`) كدالة نقية منفصلة عن استعلامات القاعدة تحديداً لتكون قابلة للاختبار بلا قاعدة بيانات — أضفت 7 اختبارات وحدة جديدة (101 → 108/108 ناجحة)، تسد بالضبط الفجوة التي وثّقها T-08 سابقاً (حسابات الضريبة غير قابلة للعزل/الاختبار في بقية المشروع).

**ممنوع الاتصال ببوابة ZATCA — التزمت بذلك**: لا استدعاء شبكي لأي بوابة إقرار، موثَّق صراحة في تعليق أعلى جدول السكيما نفسه.

`typecheck` نظيف، `pnpm run build` (esbuild) نظيف.

**⚠️ فجوة صريحة غير مُغلقة**: حاولت تطبيق الـmigration على قاعدة Neon التجريبية مرتين (`pnpm -C lib/db run migrate`) — فشلت كلتاهما بـ`ETIMEDOUT` عند `CREATE SCHEMA IF NOT EXISTS "drizzle"`، نفس قيد الشبكة الموثّق سابقاً في T-01/T-02/T-07 لهذه البيئة تحديداً نحو Neon. **الجدول غير موجود فعلياً في القاعدة الآن** — الكود يفترض وجوده ولن يعمل حتى يُطبَّق الـmigration من بيئة بشبكة طبيعية (`pnpm -C lib/db run migrate`) قبل أي استخدام فعلي لهذه الميزة.

### T-13 — توضيح حالة اعتماد ZATCA الفعلية ✅ منجزة فعلياً (PR #15، دُمجت 2026-09-03)
تحذير صريح أُضيف لـ`PILOT_LAUNCH_PLAN.md` (بعد §5أ عن ZATCA): يذكر HMAC-STUB بدل ECDSA/CSID، وأن `submitToZATCA` لا يتصل بخادم حقيقي، وأن onboarding فعلي (CSR+OTP+CSID) مطلوب قبل أول عميل حقيقي. اختبرت الدمج محلياً قبل الدفع (الفرع تفرّع من قاعدة قديمة، الدمج نظيف ويقتصر على الإضافة المقصودة فقط) وتحقّقت من وجود النص الفعلي بعد الدمج. **يصحح** المحاولة الأولى (`docs/T-13-pilot-launch-update`، PR #6) التي وثّقت شيئاً آخر خطأً باسم T-13.

### T-03 — تفعيل Sentry ✅ منجزة (PR #3، دُمجت 2026-09-03)
دُمج `errorHandler.ts` بتتبّع Sentry خلف `SENTRY_DSN`، تحقّقتُ منه بنفسي على نسخة محلية منفصلة (تثبيت + typecheck + 33/33 اختبار + build) قبل الدمج — لا اعتماد على التقرير وحده. **الباقي**: إعادة تحقق حية ضد DSN حقيقي من sentry.io عند توفره لاحقاً (ليست حاجزاً).

---

## 🟡 مهم

### T-04 — `branchId` في الـJWT ✅ منجزة (PR #12، دُمجت 2026-09-03)
تبيّن أن `branchId` كان مطبَّقاً بالفعل بالكامل عبر السلسلة (توقيع التوكن، استخراجه في auth middleware، تعبئة sales.branchId) قبل هذا الفرع — أكّدته بنفسي عبر grep مباشر في الكود. مساهمة الفرع الفعلية: اختبار تكامل فقط يثبت السلوك ويؤكد أن التوكنات القديمة بلا الحقل لا تنكسر (NULL بدل خطأ).

### T-05 — استيراد منتجات من ملف ✅ منجزة (PR #9، دُمجت 2026-09-03)
واجهة 3 خطوات (رفع→معاينة→استيراد) تستخدم `POST /products/bulk` الموجود مسبقاً — لا سطح API جديد. راجعت الكود: لا `eval`، لا `dangerouslySetInnerHTML`.

### T-06 — اختبار وضع عدم الاتصال (Offline) ميدانياً ✅ منجزة (PR #13، دُمجت 2026-09-03)
4 اختبارات تكامل ضد طبقة خدمة offline-queue الحقيقية + `docs/T06_OFFLINE_TEST_FINDINGS.md` يوثّق بصدق ما لم يُختبَر (لا محاكاة انقطاع شبكة حقيقي — لا يوجد عميل offline فعلي في المستودع، لا اختبار مصالحة سحب، فجوة ذرية موثّقة بين commit البيع وتحديث حالة الطابور).

### T-07 — ترحيل المسارات القديمة إلى بنية الموديولات ✅ اكتملت بالكامل (2026-09-11)
`src/routes/` لم يعد يحوي إلا `auth.ts`، `health.ts`، و`index.ts` (المجمِّع، أصبح شبه فارغ — يبقى مؤقتاً لأن `modules/customers`، `modules/suppliers`، `modules/sales`، `modules/purchases`، `modules/expenses`، `modules/vouchers`، `modules/accounting` لا تزال مُركَّبة عبره لا مباشرة في `app.ts`؛ هذا تنظيف صغير متبقٍ، ليس عاجلاً). كل مسار عمل تجاري آخر هُجِّر إلى `modules/<name>/{routes,services,repositories}` بنفس نمط `/brands`: `requireAuth + enforceTenantIsolation + requireActiveSubscription()` (+ `requirePermission` حيث كان موجوداً) داخل راوتر الموديول نفسه، تركيب مباشر في `app.ts`.

**المسارات المُهجَّرة (بالترتيب)**: `brands` (PR #14، من قبل) → `units` → `order-types` → `device-models` → `product-attributes` → `payment-methods` → `warehouses` (+`warehouses/sections`) → `barcode-search` → `categories` → `settings` → `onboarding` → `import` / `export` → `installment-plans` / `installment-sales` → `cash-sessions` → `products` → `repairs` → `repair-parts` → `reports`. جميعها دُفعت مباشرة على `claude/project-analysis-status-v7bh07` (بدون PR منفصل — Claude Code نفّذها كمنفّذ ومراجع في آنٍ، بنفس صرامة التحقق).

**تصادم مسارات المبيعات (البند الحرج الذي بدأت به هذه المرحلة) — مُغلَق**: تحقّق حي (سيرفر فعلي، لا قراءة كود فقط) أكّد أن `modules/sales/routes/sales.ts` (القديم) هو المسار الحي الفعلي لـ`POST /api/sales` — يدعم مسودات، دفعات جزئية/مقسّمة، ربط جلسة الصندوق، وقيود محاسبية كاملة عبر `postSaleEntry`. `modules/pos/routes/sales.routes.ts` (الموديولي) كان كوداً ميتاً فعلاً (لا يدعم إلا بيعاً مكتملاً بدفع كامل، بلا قيود محاسبية) — حُذف بالكامل مع `saleController.ts`. خدمة `modules/pos/services/saleService.ts` نفسها **ليست ميتة** — تُستخدم مباشرة من `ecommerceService.ts` و`offlineQueueProcessor.ts`، فبقيت كما هي.

**منهجية التحقق المتبعة لكل مسار مُهجَّر (بلا استثناء)**:
1. قراءة الملف القديم كاملاً أولاً، لا افتراض سلوكه.
2. بناء الموديول الجديد (route/service/repository) بنفس العقد بالضبط — لا إضافة ولا حذف حقل أو سلوك، حتى لو كان يبدو "خطأً" أو "غير متسق" (مثال: `cashSessions` يُرجع `closingBalance` كنص خام لا رقماً محوَّلاً رغم تحويل باقي الحقول — سلوك المصدر الأصلي بالحرف، لم أُصلحه أثناء الترحيل).
3. `pnpm run typecheck` (كل الـworkspace) بعد كل مسار — نظيف دائماً قبل الانتقال للتالي.
4. `pnpm run test -- --project unit` — 101/101 محافَظ عليه بعد كل مسار بلا استثناء.
5. لمسارات المعاملات (transactions) أو الحسابات المهمة: تحقّق إضافي بـ`diff`/`grep` يقارن العدد الدقيق لاستدعاءات دالة حرجة أو استخدامات متغيّر بين الملف القديم والجديد (مثال: عدّ استدعاءات `applyStockDelta` بمعاملاتها بالضبط في `repair-parts`؛ عدّ كل `Number(...)` التحويلية في حساب الربح بـ`reports` — معلَّق في الكود الأصلي كـ"load-bearing"، ليست تجميلية).
6. **منطقتا خطر فعليتان لُمستا** (راجع §4 AGENTS.md): `products` (محرك المخزون — لكن اتضح أنه كان أصلاً مجرد غلاف HTTP رقيق فوق `modules/products/services/productService.ts` الموجود مسبقاً، فالترحيل انخفض المخاطر فعلياً لا رفعها) و`repair-parts` (يستدعي `applyStockDelta` مباشرة — تحقّق المعاملات بالضبط كما في البند 5).

**تحقق نهائي شامل بعد آخر مسار (2026-09-11)**:
- `pnpm run typecheck` نظيف على كامل الـworkspace (api-server + pos-system).
- `pnpm run build` (esbuild) نجح بلا أخطاء.
- 101/101 اختبار وحدة ناجح.
- **⚠️ لم أتمكن من تشغيل السيرفر المبني حياً والتحقق عبر HTTP فعلي** في هذه الجلسة — نفس قيد الشبكة الموثّق مسبقاً في T-01/T-02 (اتصال بطيء/محجوب نحو Neon من هذه البيئة تحديداً)، ظهر هنا بشكل أوسع: حتى `curl` على `127.0.0.1` لمنفذ محلي لم يستجب رغم تأكّد بقاء العملية حيّة — على الأرجح لأن السيرفر عالق قبل `listen()` بانتظار استعلام إقلاع بطيء (مثل seeding RBAC) وليس عطلاً في الكود. **هذا يبقى الفجوة الوحيدة غير المُغلقة**: التحقق الثابت (typecheck+build+tests+مراجعة يدوية سطراً بسطر) قوي جداً، لكنه ليس بديلاً كاملاً عن طلب HTTP حي فعلي على كل مسار. يُنصح بإعادة هذا التحقق أول فرصة تتوفر فيها بيئة بشبكة طبيعية (أو نشر فعلي) قبل أي إطلاق تجريبي حقيقي.

---

## 🟢 تحسينات

### T-08 — تغطية اختبارات أوسع ✅ منجزة (PR #7، دُمجت 2026-09-03)
101 اختبار وحدة بدل 33 (+68: صلاحيات RBAC، حدود الاشتراك، سلسلة ZATCA). تحقّقتُ بنفسي بنفس الرقم تماماً (101/101) على الشجرة المدموجة. حسابات الضريبة لم تُغطَّ — مضمّنة داخل خدمات DB بلا helper نقي قابل للعزل، موثّق بصدق في رسالة الالتزام.

### T-09 — قياس الأداء ✅ منجزة جزئياً (PR #8، دُمجت 2026-09-03)
قياس فعلي لـ`createSale`: p50 ~2470ms عبر Neon سحابية — الزمن مهيمن عليه بزمن الشبكة (5-8 استعلامات متسلسلة). الهدف <300ms **غير قابل للتحقق سحابياً** بمعزل عن مكان استضافة API. Lighthouse لم يُنفَّذ — يحتاج متصفحاً ونشراً فعلياً حقيقياً.

### T-14 — جسر API لبيانات المنصة إلى لوحة تسويق موحّدة ✅ منجزة (2026-09-11) — النطاق حُدِّد ذاتياً وبشفافية (بتفويض من صاحب المشروع)
`GET /api/platform/marketing-summary` خلف `requireAuth + requirePlatformAdmin` الموجودين أصلاً (بلا `enforceTenantIsolation`، بنفس نمط بقية هذا الراوتر — يقرأ عبر كل الشركات بتصميم). الحقول التي اخترتها (موثَّقة في رسالة الالتزام لأن صاحب المشروع لم يحدّدها بنفسه صراحة، والمخاطرة منخفضة: قراءة فقط، مجمّعة، بلا أموال): `totalCompanies`، `companiesByStatus`، `companiesBySubscriptionStatus`، `companiesByPlan`، `companiesByBusinessType`. **لا بيانات حساسة تفصيلية إطلاقاً** — لا اسم شركة، لا id، لا بيانات تواصل، أعداد فقط.

يعيد استخدام `listCompanies()` الموجودة أصلاً (نفس منطق "آخر اشتراك" الذي يعتمد عليه `/api/platform/companies`) بدل تكرار الاستعلام — الملخص لا يمكن أن ينحرف عمّا تعرضه قائمة الشركات نفسها. `typecheck` نظيف، build نظيف، 108/108 اختبار وحدة (بلا اختبار جديد — تجميع بسيط فوق مصدر بيانات مُختبَر أصلاً).

### T-15 — نظام تسويق بالعمولة حقيقي ✅ المرحلة 1 منجزة برمجياً (2026-09-11) — ⚠️ الترحيل لم يُطبَّق + أجزاء متبقية خارج نطاق هذا المستودع
**القرارات المعتمدة (من صاحب المشروع مباشرة قبل الكود)**:
1. "البيع" المحتسب عليه عمولة = اشتراك شركة جديدة في ERP أو R (وليس بيع تجزئة داخل نظام POS لتاجر).
2. الصرف يدوي بالكامل — النظام يتتبّع المستحق وحالته (pending/approved/paid) فقط، بلا أي تكامل دفع فعلي.
3. البيانات المركزية كان مخطَّطاً لها في spruvex-s، لكن تحقّقت أن ذلك المستودع حالياً واجهة ثابتة (Vite+React) بلا أي خادم أو قاعدة بيانات إطلاقاً — "قاعدة مركزية هناك" يعني بنية تحتية جديدة كاملة (Neon جديدة + API + قرار استضافة)، لا أستطيع تزويدها بنفسي (يحتاج تدخل صاحب المشروع اليدوي في لوحة تحكم كما حدث سابقاً مع قاعدة الاختبار). بتفويض "اتخذ الإجراء الأفضل": بنيتها داخل قاعدة بيانات ERP نفسها (البنية التحتية الوحيدة تحت سيطرتي الكاملة) — spruvex-s وR يصلان إليها فقط عبر HTTP، لا وصول مباشر لقاعدة البيانات أبداً، فيبقى مبدأ العزل الكامل محفوظاً من الناحية العملية حتى لو لم تكن البيانات حرفياً في spruvex-s. قابل للنقل لخدمة مستقلة لاحقاً بلا تغيير العقد الذي يراه الطرف الآخر.

**ما بُني**: `modules/affiliates` جديد كامل — جدولان (`affiliates`, `referral_conversions`، migration `0028`، تحققت: جدولان جديدان فقط)، `POST /api/affiliates/report-conversion` (نقطة دخول بين المنتجات، بدون `requireAuth` — محمية بمفتاح سري في الترويسة يُتحقَّق داخل الـhandler، فشل مغلق: مفتاح غير مضبوط = رفض الجميع، ليس قبول الجميع)، إدارة كاملة خلف `requirePlatformAdmin` الموجود أصلاً (`GET/POST /api/platform/affiliates`, `GET /api/platform/conversions`, `POST .../approve`, `POST .../mark-paid`). العمولة تُحسب من سعر الخطة الحقيقي في `PLAN_CATALOG` (لا سعر مُخترَع)، وتُجمَّد على كل سجل عند التسجيل (تغيير النسبة لاحقاً لا يُغيّر ديناً مُسجَّلاً بأثر رجعي). فهرس فريد `(product, externalCompanyId)` يمنع الإحالة المزدوجة فعلياً على مستوى القاعدة (تحققت أن الخدمة تلتقط خطأ التفرّد، لا تعتمد على فحص في الذاكرة فقط). تسجيل الشركة في ERP نفسه (`authService.registerCompany`) يقبل الآن `referralCode` اختيارياً ويُبلّغ عن التحويل تلقائياً بعد نجاح التسجيل — بنفس نمط "لا يوقف التسجيل الناجح أبداً" المستخدم فعلاً لإرسال بريد الترحيب (try/catch + تسجيل تحذير فقط).

الصف يبدأ `pending` دائماً (الشركة تبدأ بفترة تجريبية 14 يوماً عند التسجيل، ليست اشتراكاً مدفوعاً فوراً) — المراجعة اليدوية (approve ثم mark-paid) هي بالضبط حيث يُحسم "هل تحوّلت فعلاً لعميل مدفوع"، وليس عند لحظة التسجيل. `computeCommission` دالة نقية منفصلة، 5 اختبارات وحدة جديدة (113/113 إجمالاً). `typecheck` نظيف، build نظيف.

**لم يُبنَ عمداً في هذه المرحلة (خارج نطاق هذا المستودع أو يحتاج قراراً لاحقاً)**: تتبّع النقرات، واجهة رابط الإحالة على أي من المواقع الثلاثة (هذا نطاق Manus لا هذا المستودع — الـAPI جاهز ليُستدعى)، جانب SpruVex R من الإبلاغ عن تحويل (مستودع منفصل تماماً، خارج نطاق هذه الجلسة)، أي أتمتة دفع/صرف فعلية.

**⚠️ نفس فجوة T-12/T-16/T-07 بالضبط**: migration `0028` لم يُطبَّق على القاعدة الفعلية بعد — نفس قيد الشبكة البيئي.

### T-16 — تأسيس وحدة الموارد البشرية (HR) ✅ منجزة برمجياً (2026-09-11) — ⚠️ الترحيل لم يُطبَّق على القاعدة بعد (نفس قيد T-12)
`modules/hr` جديد (routes/services/repositories، بلا validators/controllers منفصلة — التحقق مضمّن في الخدمات مباشرة، بنفس نمط كل الموديولات البسيطة الأخرى المُهجَّرة هذه الجلسة). 3 جداول جديدة فقط (migration `0027` — تحققت: `departments`/`employees`/`attendance` فقط، لا شيء آخر تغيّر): `departments` (name + managerId اختياري)، `employees` (منفصل عمداً عن `users` — أضفت `employees.userId` اختياري بدل لمس جدول `users` المركزي للمصادقة، وهو الاتجاه الأقل اضطراباً)، `attendance` (clockIn/clockOut/source).

صلاحيتان جديدتان `HR_VIEW`/`HR_MANAGE` أُضيفتا لقائمة `PERMISSIONS` — admin/owner/manager يحصلون عليهما تلقائياً (يمنحون كل الصلاحيات)، بقية الأدوار (cashier، إلخ) لا، بلا تعديل عليها. تحققت أن `rbacCatalog.unit.test.ts` لا يزال ينجح بلا تعديل (يفحص التكامل المرجعي لا عدداً ثابتاً).

منطق حقيقي بسيط في `attendanceService`: لا يمكن تسجيل حضور لموظف مسجّل حضوره فعلاً بلا انصراف (409)، ولا تسجيل انصراف مرتين لنفس السجل (409) — بدل الكتابة الصامتة فوق البيانات.

**ممنوع بالكامل ولم يُلمس**: رواتب، بدلات، إجازات، أي اتصال بـ`modules/accounting`.

`typecheck` نظيف، `pnpm run build` نظيف، 108/108 اختبار وحدة (بلا اختبارات جديدة — منطق هذه الوحدة CRUD بسيط + شرطان، لا يستدعي اختباراً معزولاً بخلاف حساب VAT في T-12).

**⚠️ نفس فجوة T-12 بالضبط**: حاولت تطبيق migration `0027` على قاعدة Neon التجريبية — نفس قيد الشبكة البيئي. الجداول غير موجودة فعلياً بعد؛ يلزم `pnpm -C lib/db run migrate` من بيئة بشبكة طبيعية قبل أي استخدام فعلي.
**نفّذها على أكثر من فرع إذا لزم** (مثلاً: فرع للجداول + الموديول الأساسي، فرع منفصل للحضور) بدل فرع ضخم واحد.

### T-10 — تنظيف التوثيق القديم ✅ منجزة (PR #5، دُمجت 2026-09-03)
`spruvex-app/CLAUDE.md` حُذف بالكامل — `AGENTS.md` هو المرجع الآن.

---

### T-17 — تدقيق شامل (رحلة التاجر، المنتجات، شاشة POS، الطباعة، الترابط) + إصلاحات فورية ✅ الدفعة الأولى منجزة (2026-09-11)

بتكليف مباشر من صاحب المشروع: "راجع كل شي و طور اي شي يحتاج الى تطوير و قم بجميع الاضافات" على 5 محاور محددة. المنهجية: 4 وكلاء Explore متوازية (كل واحد قرأ كوداً حقيقياً سطراً بسطر مع أرقام أسطر، لا افتراضات) + اختبار حي فعلي بـPlaywright لواجهة تسجيل الدخول/التسجيل (الجزء المعتمد على قاعدة بيانات حية بقي محجوباً بنفس قيد الشبكة نحو Neon الموثَّق في كل مهمة سابقة) + بحث مقارن مع Foodics/Zid/Salla/Loyverse/Square/Odoo. النتائج الكاملة (استشهادات ملف:سطر) محفوظة في محادثة الجلسة؛ هنا الخلاصة القابلة للتنفيذ فقط.

**إصلاحات حقيقية نُفِّذت وتحقّقت في هذه الدفعة** (typecheck نظيف على كامل الـworkspace، build نظيف، 113/113 اختبار وحدة، بلا تغيير سلوك غير مقصود):

1. **قالب POS "قائمة" (List) كان يتجاهل مجموعات الإضافات (addons) تماماً** — أخطر خلل حقيقي وُجد: تاجر على قالب List (افتراضي لتجزئة/بقالة/تصليح) يُضيف مجموعات إضافات على منتج (متاح دائماً من `AddonManager` بصرف النظر عن نوع النشاط) يجد أن هذه الإضافات **غير قابلة للوصول إطلاقاً عند نقطة البيع** — تُضاف بالسعر الأساسي بصمت. أُصلح: `ListPosTemplate.tsx` أصبح يفحص `product.hasAddons` ويفتح `AddonPickerDialog` بنفس منطق Grid/Image/Mobile بالضبط (بما فيها الحفاظ على السطر منفصلاً لكل تركيبة إضافات/ملاحظة مختلفة بدل الدمج).
2. **حركات المخزون الناتجة عن مشتريات كانت تُعرض بشارة بلا لون + إشارة سالبة (-) خاطئة + نص غير مترجم** في شاشة `/inventory/movements` — التاجر الذي يفتح الشاشة للتحقق من أن مشترياته زادت المخزون فعلاً يرى إشارة ناقص حمراء مضلِّلة ونص `inventory_movements.type_purchase` الخام. أُصلح: أُضيف `purchase` لجدولي `MOVEMENT_BADGE` و`isPositive`، وأُضيف مفتاح `type_purchase` في `en.json`/`ar.json`.
3. **`ecommerceModuleEnabled` محسوب فعلياً في الباك-إند لكن غير موجود إطلاقاً في عقد الـAPI المُولَّد** (`openapi.yaml` → orval → `api-client-react`) — لذلك عنصر تنقّل "الطلبات الإلكترونية" كان يظهر لكل تاجر بلا استثناء حتى من لم يفعّل التجارة الإلكترونية إطلاقاً (نمط "نصف مربوط": الحقل محسوب ومحفوظ، لكن لا كود frontend يقرؤه لأن نوعه غير معلن). أُصلح من الجذر: أُضيف `ecommerceModuleEnabled` إلى `Settings`/`SettingsInput` في `openapi.yaml`، أُعيد توليد `api-client-react`/`api-zod` عبر `orval`، ثم `sidebar.tsx` أصبح يُخفي "الطلبات الإلكترونية" فعلياً حين `settings.ecommerceModuleEnabled === false` (بنفس نمط إخفاء "الصيانة" الموجود أصلاً لـ`repairsModuleEnabled`).
4. **شاشة الأقسام (`/inventory/categories`) الوحيدة بلا بحث** رغم وجوده في المنتجات والماركات (تضارب تجربة مباشر بين شاشات شقيقة) — أُضيف بحث فوري (اسم عربي/إنجليزي) يُبقي القسم الرئيسي ظاهراً إن طابق هو أو أحد أبنائه، ويُظهر رسالة "لا توجد نتائج" منفصلة عن "لا توجد أقسام أصلاً". كما أن **صورة القسم المرفوعة كانت غير مرئية إطلاقاً في القائمة** (تُحفظ في الحوار لكن الصفوف تعرض أيقونة ثابتة فقط) — أُصلح بعرض `imageUrl` كصورة مصغّرة حين تكون موجودة.

**الدفعة الثانية من الإصلاحات ✅ منجزة (2026-09-12)** — نفس انضباط التحقق (typecheck نظيف على كامل الـworkspace، build نظيف، 113/113 اختبار وحدة):

5. **لا فلاتر فئة/مخزون منخفض في قائمة المنتجات** رغم دعم الباك-إند الكامل لهما (`categoryId`, `lowStock`) — أُضيف `<Select>` للفئة و`<Switch>` لـ"منخفض المخزون فقط" بجانب مربع البحث في `/inventory`. **الفلترة حسب الماركة لم تُضَف** — تحققت أن الباك-إند لا يدعمها أصلاً (`products.brand` نص حر بلا علاقة، لا معامل فلترة له في `productRepository.ts`)، فلا يوجد ما يُربَط به بعد؛ يبقى بند مستقل.
6. **لا زر "إعادة محاولة" عند فشل تعبئة الكتالوج التجريبي** في معالج الإعداد — تحققت أن نقطة النهاية `POST /onboarding/seed-catalog` idempotent فعلاً بتصميمها (تعليق الكود الأصلي يقول ذلك صراحة: كل استدعاء يُنشئ مجموعة بلاحقة SKU مختلفة، لا يفشل بتكرار). أُضيف زر "إعادة المحاولة" في شاشة "done" يظهر فقط عند فشل التعبئة، يعيد استدعاء نفس الدالة دون الرجوع خطوة للخلف.
7. **لا شريط تجريبي/عد تنازلي لفترة الـ14 يوماً** — `trialEndsAt` كان محسوباً ومحفوظاً في الباك-إند (على الشركة والاشتراك معاً) لكن غير مُعرَّض عبر أي مسار يصل إليه التاجر نفسه (فقط `platform` الداخلي خلف صلاحية مدير المنصة). أُضيف مسار جديد للتاجر نفسه `GET /api/subscriptions/status` (خلف `requireAuth + enforceTenantIsolation` فقط، بلا أي صلاحية إضافية — يقرأ حالة شركته هو فقط) يُعيد `{status, plan, trialEndsAt, currentPeriodEnd}` بإعادة استخدام `getEffectiveState` الموجود أصلاً (نفس مصدر الحقيقة المستخدم في كل middleware آخر، لا استعلام جديد). أُضيف للمخطط في `openapi.yaml` وأُعيد توليد `api-client-react`. مكوّن جديد `TrialBanner.tsx` يُركَّب داخل `AppLayout` (يظهر في كل صفحة تحت الـTopbar): شريط كهرماني عند تبقّي أكثر من 3 أيام، أحمر عند 3 أيام أو أقل أو عند اليوم الأخير، وشريط أحمر منفصل عند `status === "expired"`. لا شيء يُعرض لبقية الحالات (active/suspended/cancelled) — خارج نطاق هذا الإصلاح.

**متبقٍ من نفس القائمة (لم يُلمَس بعد)**:
- تكرار 4 عناصر تنقّل بمسميات مختلفة تشير لنفس الصفحة بالضبط (`/pos`, `/accounting`, `/vouchers`, `/reports`)، و`/settings` تتكرر 3 مرات — يستحق قرار تصميم IA لا إصلاحاً آلياً.
- خطوة دعوة موظفين غائبة تماماً من التسجيل/المعالج (لا يوجد أصلاً "invite" في المنتج كله).
- تحقق OTP الفعلي يحدث فقط عند الإرسال النهائي (بعد اختيار نوع النشاط والخطة) بدل التحقق مباشرة بعد إدخاله — قد ينتهي صلاحية الرمز (10 دقائق) دون تنبيه فوري.
- عملة الشركة مثبتة على SAR دائماً بلا أي خطوة اختيار عند التسجيل، رغم وجود حقل `currency` كامل في الإعدادات.

**الدفعة الرابعة ✅ منجزة (2026-09-12) — فجوة صلاحيات حقيقية، ليست تجميلية فقط**:

8. **مسارات إنشاء/تعديل/حذف الأقسام والماركات كانت بلا أي فحص صلاحية إطلاقاً في الباك-إند** (`categories.routes.ts`, `brands.routes.ts`) — أي مستخدم مصادَق عليه في الشركة (حتى كاشير بلا أي صلاحية `products.*`) كان يستطيع إنشاء/تعديل/حذف قسم أو ماركة عبر استدعاء API مباشر، بصرف النظر عن دوره — الواجهة كانت تُخفي/تُظهر الأزرار بلا قيد أيضاً فتوافقت الفجوتان. أُصلح: أُضيف `requirePermission(PRODUCTS_CREATE/UPDATE/DELETE)` على مسارات POST/PUT/DELETE في كلا الموديولين — بنفس الصلاحيات المستخدمة فعلاً في `products.routes.ts` (الأقسام والماركات بيانات وصفية للمنتج، لا نطاق منفصل). كما أُضيف نفس القيد لأزرار الواجهة في `categories.tsx`/`brands.tsx` (كانت الأزرار تُعرض دائماً بلا شرط) وأُضيف قيد `products.update` المفقود على زر تعديل المنتج نفسه في `/inventory` (كان الوحيد بلا قيد رغم أن الحذف مقيَّد أصلاً).

    **⚠️ فجوة مكتشفة أثناء هذا الإصلاح، لم أُصلحها — تحتاج قراراً من صاحب المشروع**: دور `inventory_staff`/`warehouse_staff` في `roles.ts` يملك الصلاحيات القديمة المسطّحة (`ADD_PRODUCT`, `MANAGE_INVENTORY`) لكن **لا يملك الصلاحيات الجديدة المنقّطة** (`products.create/update/delete`) التي يتحقق منها فعلياً كل من `products.routes.ts` وموديولَي الأقسام/الماركات بعد إصلاحي. أي أن موظف المخزون **لا يستطيع أصلاً إنشاء/تعديل/حذف منتج** تحت النظام الحالي — وإصلاحي اليوم يمدّ نفس القيد (الصحيح أمنياً، لكنه غير مكتمل عملياً) للأقسام والماركات أيضاً. لم أُعدّل `DEFAULT_ROLES` لإصلاح هذا لأن: (أ) الشركات الموجودة فعلاً على القاعدة الحقيقية لديها صفوف أدوار مُخزَّنة مسبقاً من القالب القديم، وتعديل `DEFAULT_ROLES` هنا يؤثر فقط على شركات جديدة مستقبلاً لا يُصحِّح الموجود، (ب) لا أملك وصول قاعدة بيانات حي في هذه البيئة للتحقق من هذا الافتراض ميدانياً أو لتشغيل أي migration تصحيحي لازم. **القرار المطلوب من صاحب المشروع**: هل يُراد منح `inventory_staff`/`warehouse_staff` صلاحيات `products.create/update/delete` المنقّطة (يطابق نيّة الاسم/الوصف الظاهرة)، أم أن تقييدهم الحالي (حذف كامل) مقصود فعلاً؟ عند الحسم، يلزم كتابة migration/سكربت لتحديث صفوف `role_permissions` للشركات الموجودة فعلاً، لا تعديل `DEFAULT_ROLES` وحده.

**متوسط الجهد (يحتاج تصميماً بسيطاً + تعديل frontend محدود، بلا تغييرات بنيوية)**
- ~~معرض صور متعدد للمنتج~~ — **✅ منجزة بالكامل (2026-09-12)**: الجدول `product_images` كان موجوداً في المخطط أصلاً بلا أي مسار API أو واجهة. أُضيف كاملاً الآن: 4 مسارات جديدة `GET/POST /products/:id/images`، `PUT/DELETE /products/:id/images/:imageId` (بنفس نمط مسارات الوحدات الفرعية الأخرى للمنتج — variants/related/units/batches — بلا توثيق openapi.yaml، بما يطابق أن تلك المسارات الشقيقة أيضاً غير موثّقة هناك أصلاً). تبويب جديد "معرض الصور" في `manage.tsx` (`ImagesTab.tsx`) يعيد استخدام `MediaUploadField` الموجود أصلاً — رفع، حذف، تعيين "صورة رئيسية" (يُلغي الرئيسية عن البقية تلقائياً عبر `clearPrimaryImage` بدل الاعتماد على فحص في الذاكرة)، وإعادة ترتيب فعلية (تبديل `sortOrder` مع الجار، أزرار يمين/يسار مقلوبة صح مع RTL). **تحقّق حي كامل عبر Playwright ضد قاعدة PostgreSQL محلية حقيقية** (لا قراءة كود فقط): تسجيل شركة → إنشاء منتج → فتح تبويب المعرض → رفع صورتين فعليتين (PNG حقيقي) → ظهورهما في الشبكة مع أزرار الترتيب/التمييز/الحذف — لقطة الشاشة محفوظة في الجلسة، ونجاح فعلي لكل من `POST`/`GET /products/:id/images` (201/200) في سجلات الخادم الحي. `typecheck` نظيف، `build` نظيف، 113/113 اختبار وحدة.
- ~~شجرة أقسام حقيقية~~ — **✅ منجزة (2026-09-12)**: كانت الشاشة تعرض مستويين فقط بالضبط (رئيسي + فرعي)، والقائمة المنسدلة للأب لا تسمح باختيار قسم فرعي كأب رغم أن `categories.parentId` في المخطط مرجع ذاتي بلا حد عمق إطلاقاً. أُعيد بناء `categories.tsx` كشجرة حقيقية غير محدودة العمق: مكوّن `CategoryNode` تكراري (recursive) يعرض أي عدد من المستويات بإزاحة (indentation) تتزايد مع العمق، وزر "إضافة قسم فرعي" متاح عند كل مستوى وليس الجذر فقط. القائمة المنسدلة للأب في نافذة الإضافة/التعديل تسمح الآن باختيار **أي** قسم كأب، مع استبعاد القسم نفسه وكل أحفاده (`descendantIds` تكرارية) لمنع إنشاء دورة (cycle) من الواجهة مباشرة. أُضيف أيضاً تحقق مطابق في الباك-إند (`categoryService.updateCategory`): يمشي فعلياً في سلسلة أسلاف الأب الجديد المقترح (لا فحص السطح المباشر فقط كما كان سابقاً) ويرفض أي دورة أعمق (مثال: نقل قسم تحت أحد أحفاده). **تحقّق حي كامل عبر Playwright ضد PostgreSQL محلي حقيقي**: أنشأت 3 مستويات فعلية (إلكترونيات ← جوالات ← آيفون) وظهرت جميعها بإزاحة صحيحة متدرجة في الواجهة — لقطة الشاشة محفوظة في الجلسة. `typecheck` نظيف، `build` نظيف، 113/113 اختبار وحدة.
- لا صفحة إعدادات مخصصة لضبط ZATCA (CSR/CSID/حالة الاعتماد) — الحقل الوحيد الظاهر هو الرقم الضريبي؛ التكامل يعمل تلقائياً من شاشة المبيعات بلا أي شاشة إدارة مقابلة.
- لا معاينة حقيقية للفاتورة قبل الطباعة الفعلية (المعاينة الموجودة في منشئ القوالب بيانات وهمية ثابتة فقط) — النقر يطبع مباشرة بعد 300ms.
- عمليات جماعية (bulk) على قائمة المنتجات: لا تحديد صفوف، لا حذف/تعديل فئة جماعي.
- ~~توحيد أداتي الاستيراد/التصدير المزدوجتين للمنتجات~~ — **رابط عابر ✅ مُضاف (2026-09-12)**: التوحيد الفعلي (دمج الأداتين في واحدة) يبقى إعادة تصميم أكبر مؤجلة، لكن الفجوة الأخطر (تاجر لا يكتشف الأداة الأقوى إطلاقاً) أُغلقت بسطر ربط في `inventory/import.tsx` يشير لـ`/settings/import-export` مع شرح مختصر لما تقدّمه إضافياً (ربط أعمدة يدوي، معالجة تكرارات، تصدير) — تحققت أن الصفحة الوجهة غير مقيدة بصلاحية admin فقط (`adminOnly` غير مضبوطة على مسارها في `App.tsx`)، فالرابط صالح لأي دور يصل أصلاً لـ`/settings`.

**استراتيجي/أكبر جهداً (يحتاج قراراً من صاحب المشروع قبل البدء، أو بنية تحتية جديدة)**
- طباعة حرارية حقيقية (ESC/POS عبر USB/Bluetooth/شبكة) بدل `window.print()` + CSS فقط — لا فتح درج نقدي، لا طابعة مطبخ منفصلة عن فاتورة العميل، لا طباعة ملصقات باركود للمنتجات. هذا الفارق الأكبر مقارنة بـFoodics/Square/Loyverse، لكنه يتطلب تطبيق سطح مكتب/جسر أجهزة حقيقي — ليس عمل frontend/backend عادياً.
- وحدة BI الكاملة (`modules/bi` — 10 نقاط نهاية: أفضل/أسوأ منتجات، أداء الفروع/المستخدمين، تحليل المصروفات، التدفق النقدي) **مبنية بالكامل في الباك-إند ولا تُستدعى من الواجهة إطلاقاً** — التقارير الحالية تستخدم موديول `reports` الأبسط والمنفصل بدلاً منها. فرصة كبيرة بأقل جهد ممكن (الـAPI جاهز 100%) — يستحق أولوية أعلى من "استراتيجي" فعلياً، لكن يحتاج تصميم شاشات تقارير جديدة.
- واجهة الوحدات المبنية في الباك-إند بلا أي واجهة مستخدم إطلاقاً: `affiliates` (نظام العمولة الكامل من T-15 — لوحة موافقة/صرف)، `ai` (مساعد منتج/ملخص أعمال)، `subscriptions`، `sync` (مزامنة أوفلاين)، `support` (تذاكر دعم)، `platform` (لوحة تشغيل SaaS داخلية) — بعضها بتصميم مقصود (platform لوحة إدارة داخلية لا تخص التاجر)، بعضها فجوة حقيقية (affiliates، support).
- توسيع Manage Hub (`manage.tsx`) ليضم `AddonManager` (حالياً معزول داخل شاشة تعديل المنتج فقط، بلا علاقة منطقية بمكانه الحالي مقارنة بتبويبات Variants/Related/Units/Batches المجاورة مفاهيمياً).

**✅ فجوة التحقق الحي أُغلقت جزئياً (2026-09-12) — اكتشاف مهم حول السبب الحقيقي لعطل "الخادم لا يستجيب"**: طوال هذه الجلسة (منذ T-01/T-02) كان يُفترض أن فشل الوصول لخادم API المُشغَّل حياً سببه بطء/انقطاع شبكة هذه البيئة تحديداً نحو Neon. **هذا الافتراض على الأرجح كان خاطئاً بالكامل.** وجدت أن PostgreSQL 16 مثبَّت فعلياً في هذه البيئة (`postgresql-16` عبر apt) — شغّلته محلياً (`service postgresql start`)، طبّقت عليه **كل الـmigrations المعلَّقة دفعة واحدة (0026 حتى 0029)** بنجاح فوري (بلا أي بطء شبكة، لأنه محلي)، وشغّلت خادم الـAPI فعلياً ضده. **النتيجة: نفس عرض "Server listening" في السجل ثم رفض الاتصال بالضبط كما كان يحدث دائماً مع Neon** — رغم أن قاعدة البيانات المحلية سريعة جداً ولا علاقة لها بالشبكة الخارجية إطلاقاً. تتبعت السبب فعلياً: `server.ts` يربط الخادم بـ`app.listen(env.port, "::", ...)` (IPv6 dual-stack، مقصود لبيئة Fly.io حسب تعليق الكود). بيئة الحاويات هذه (sandbox) **لا تدعم IPv6 إطلاقاً** (تحقّقت: `/proc/net/tcp6` غير موجود، واختبار Node مباشر يُرجع `EAFNOSUPPORT` عند الربط بـ`::`). هذا يعني أن العطل المتكرر طوال الجلسة قد يكون **بيئياً في هذا الـsandbox تحديداً (نقص دعم IPv6) لا مشكلة شبكة نحو Neon كما افتُرض** — لم ألمس `server.ts` نفسه (الربط بـ`::` قرار نشر مقصود لـFly.io، ليس خطأ يستحق تغييره بلا معرفة بيئة الإنتاج الفعلية)، لكن عدّلت نسخة محلية فقط من الملف المبني (`dist/index.mjs`, غير مُتتبَّع في git) لتربط بـ`0.0.0.0` بدلاً من `::` لغرض الاختبار المحلي فقط، ونجح الاتصال فوراً. **إن تكرر عطل "الخادم لا يستجيب" مستقبلاً في أي بيئة CI/نشر جديدة، هذا أول شيء يستحق التحقق منه قبل افتراض مشكلة شبكة نحو قاعدة البيانات.**

بهذا نجحت أول جلسة اختبار حي فعلي كامل هذه الجلسة: تسجيل شركة جديدة فعلياً (بما فيها قراءة رمز OTP من سجلات الخادم مباشرة لأن لا مزوّد بريد حقيقي مُهيَّأ)، تسجيل الدخول، معالج الإعداد، ولوحة التحكم/المخزون/الأقسام/نقطة البيع/الإعدادات/التقارير — كلها بالعربية مع بيانات حقيقية من قاعدة بيانات حقيقية (محلية، وليست Neon الإنتاجية بعد — التحذير القائم من قبل يبقى: **migrations 0026-0029 لا تزال غير مُطبَّقة على قاعدة Neon الفعلية**، هذا التطبيق كان على نسخة محلية للاختبار فقط).

**خللان حقيقيان إضافيان اكتُشفا واعالجا خلال هذا الاختبار الحي (غير قابلين للاكتشاف بالقراءة الثابتة للكود)**:
1. مفتاح ترجمة مفقود فعلياً `inventory.no_products_desc` في كلا ملفي اللغة (كان موجوداً باسم مشابه لكن داخل مساحة اسم `pos` الخطأ بالمصادفة القريبة في نفس الملف) — ظهر كنص خام `inventory.no_products_desc` في شاشة المخزون الفارغة. أُصلح بإضافة المفتاح الصحيح في كلا الملفين.
2. (مذكور في تحديث سابق أعلاه) أسماء الباقات الإنجليزية الخام في معالج التسجيل بالعربي.

**ما لا يزال غير قابل للتحقق الحي**: بيانات حقيقية عبر عدة جلسات/فروع، تدفقات الدفع الفعلية (بوابات تابي/تمارا)، إرسال بريد فعلي (Resend)، وأي شيء يعتمد على Neon نفسها تحديداً (بيانات الإنتاج الفعلية). الاختبار المحلي يغطي المنطق والواجهة، لا التكامل مع خدمات خارجية حقيقية.

---

### T-18 — التهيئة التلقائية حسب النشاط التجاري (Business-Type Auto-Configuration) ✅ المرحلة 1 منجزة (2026-09-12) — ⚠️ Migration 0029 لم يُطبَّق بعد

**الطلب**: "تاجر يختار النشاط التجاري (مقاولات، بقالة، جوالات وصيانة...) ويكون النظام مُجهَّزاً مناسباً له تلقائياً من أول لحظة، بدل أن يبني كل شيء يدوياً" + بحث ومقارنة مع أنظمة ERP عالمية وسعودية.

**البحث الذي استند إليه القرار**: وزارة التجارة السعودية تصنّف الأنشطة عبر **ISIC4** (آلاف الرموز الفرعية) — تفصيل غير مناسب إطلاقاً لمعالج تسجيل بسيط. المقارنون الفعليون (Zid, Salla, Foodics, Odoo) يستخدمون جميعاً عدداً صغيراً من "الأنشطة/القوالب" المختارة يدوياً عند الإعداد (Odoo: تطبيقات/قوالب صناعية جاهزة؛ Foodics: مطاعم فقط بعمق كامل — قوائم، مطبخ، فروع؛ Zid/Salla: تجارة إلكترونية). القرار: توسيع نفس آلية `businessType` الموجودة أصلاً (قائمة صغيرة مُدارة، لا ISIC4) بدل استبدالها ببنية جديدة، مع تعميقها لتُهيّئ أكثر من مجرد `posTemplate`.

**فجوة جذرية اكتُشفت أثناء التنفيذ ومنعت دعم "مقاولات" بشكل حقيقي قبل حلّها**: لا يوجد أي مفهوم "خدمة" (Non-stock item) في `products` — كل صف منتج يتطلب كمية مخزون، وكل مسار بيع (`salesService.ts` بثلاث نقاط، `pos/services/saleService.ts`) يرفض البيع فوراً بخطأ "Insufficient stock" بمجرد أن يصل المخزون لصفر. هذا يعني أن نشاطاً خدمياً بحتاً (مقاولات، صالونات، استشارات) **لا يمكن تمثيله إطلاقاً** بالنظام الحالي — بند "معاينة موقع" أو "أجرة تركيب" سيكون دائماً "نافد المخزون" ولا يُباع أبداً. هذه فجوة حقيقية بمعزل عن هذا الطلب أيضاً — كل نظام قورن به (Odoo، QuickBooks، Zid، Salla، Wafeq، Daftra) يفرّق بين "منتج" و"خدمة".

**ما بُني (دفعتان)**:

**١) `products.isService` — عمود جديد (migration 0029)**: بند غير مخزّني، قابل للبيع دائماً. عُدِّلت 3 نقاط تحقّق/خصم مخزون في `salesService.ts` (إنشاء بيع، اعتماد مسودة، إرجاع/استبدال) بالإضافة لـ`pos/services/saleService.ts` (يُستخدم من التجارة الإلكترونية وطابور العمل غير المتصل) لتجاوز أي بند خدمي تماماً بدل معاملة "مخزون=صفر" الدائم كـ"نافد". **لم تُلمَس عمداً**: المشتريات (شراء "خدمة" لإدخالها كمخزون ليس سيناريو حقيقياً) وقطع الصيانة (فعلياً مادية دائماً). نموذج إضافة/تعديل منتج يعرض الآن مربع اختيار "هذه خدمة" يُخفي حقلي الكمية وحد التنبيه عند تفعيله؛ قائمة المخزون تعرض شارة "خدمة" بدل رقم المخزون ولا تُصنَّفه أبداً كمنخفض المخزون.

**٢) توسيع أنواع النشاط التجاري من 9 إلى 13**: أُضيفت 4 أنواع جديدة — اثنان بطلب مباشر من صاحب المشروع (`mobile_repair` "محل جوالات وصيانة"، `contracting` "مقاولات وإنشاءات") واثنان بقرار مهني مبرَّر بالبحث أعلاه لإثبات أن الآلية عامة لا مُخصَّصة لمثالين فقط (`pharmacy` "صيدلية"، `salon_beauty` "صالون ومركز تجميل"). كل نوع جديد يُهيّئ فعلياً (لا مجرد تسمية): `posTemplate` مناسب (`mobile_repair`→mobile، `contracting`→list، `pharmacy`→list، `salon_beauty`→grid)، `repairsModuleEnabled`/`enabledModules` (`mobile_repair` فقط يُفعِّل الصيانة)، وكتالوج بداية حقيقي بمنتجات/خدمات نموذجية لكل نوع (مثال: `contracting` يُزرَع بخدمتين فعليتين `isService: true` — "معاينة وتقييم الموقع" و"أعمال توريد وتركيب" — وليس منتجاً وهمياً واحداً كباقي الأنواع القديمة). كما صُحِّح خلل تسمية موجود مسبقاً: `electronics` كانت تُعرض للمستخدم فعلياً باسم "محل جوالات" (Mobile Shop) بالعربي والإنجليزي رغم أنه اسم عام لأي متجر إلكترونيات — أُعيدت تسميته لـ"متجر إلكترونيات"/"Electronics Store"، والاسم الصحيح "محل جوالات وصيانة" أصبح الآن نوعاً منفصلاً حقيقياً يُفعِّل الصيانة تلقائياً.

**التحقق**: `typecheck` نظيف على كامل الـworkspace، `build` نظيف، 113/113 اختبار وحدة، **واختبار حي فعلي بـPlaywright** (وليس قراءة كود فقط) أكّد أن شاشة التسجيل خطوة 3 تعرض فعلاً كل الأنواع الـ13 بأيقونات مميزة وتخطيط سليم بلا كسر بصري — لقطة الشاشة محفوظة في الجلسة.

**⚠️ نفس فجوة كل migration سابق هذه الجلسة**: migration `0029` (`products.is_service`) لم يُطبَّق على القاعدة الفعلية بعد — نفس قيد الشبكة البيئي نحو Neon.

**لم يُبنَ عمداً في هذه المرحلة (نطاق أكبر، يحتاج قراراً لاحقاً)**:
- **إدارة مشاريع/مقاولات حقيقية** (BOQ، فوترة بنسبة الإنجاز، مقاولو باطن، ربط تكلفة بمشروع) — `contracting` هنا يحصل فقط على تهيئة أذكى + قدرة بيع بنود خدمية، وليس وحدة "مشاريع" كاملة؛ هذه أكبر بكثير من "تهيئة تلقائية" وتحتاج تصميم منتج مستقل.
- توسيع `isService` ليشمل المشتريات (شراء بند خدمي كمصروف بدل مخزون) — لم يُطلب ولا سيناريو واضح له حالياً.
- ربط `businessType` بقالب محاسبي مختلف (شجرة حسابات مُخصَّصة لكل نشاط) — `ensureChartOfAccountsSeeded` الحالي عام لكل الأنواع؛ Odoo نفسه يفرّق حسب الدولة لا النشاط، فهذا ليس نمطاً معيارياً واضحاً يستحق تقليده الآن.
- توثيق `businessType`/`isService` في `openapi.yaml` لحقل `Settings.businessType` (لا يزال غير موثَّق في المخطط أصلاً من قبل هذه الجلسة — فجوة توثيق صغيرة موجودة مسبقاً، ليست من هذا العمل، لم تُصلَح).

---

## ⏸️ مؤجَّل عمداً — لا تعمل عليها

| البند | سبب التأجيل |
|---|---|
| تفعيل تكاملات سلة/زد/Shopify فعلياً | تحتاج حسابات مطوّر حقيقية |
| تفعيل تابي/تمارا/Moyasar فعلياً | تحتاج حسابات تاجر حقيقية |
| فواتير PDF/A-3 | تخص المرحلة الثانية من ZATCA — الطباعة الحالية تغطي الالتزام الحالي |
| المواقع التسويقية | انتقلت لمستودع `spruvex-s-erp-r` |
| قرارات الاستضافة والنطاق | قرار مالك المشروع |
| واجهة لوحات BI ولوحة الذكاء الاصطناعي | الأساس (API) مكتمل — الواجهة بانتظار أولوية |
| **المرحلة 4: ZATCA Phase 2 الفعلي** (توقيع ECDSA حقيقي + CSID + اتصال فعلي بخادم الهيئة، بدل الـHMAC-STUB الحالي الموثَّق في T-13) | يتطلب أولاً إجراءً إدارياً خارجياً من صاحب المشروع (فتح حساب في بوابة Fatoora، إصدار CSR، الحصول على CSID) — **ليست مهمة برمجية قابلة للجدولة الآن**. عند إتمام هذا الإجراء إدارياً، تُفتح مهمة برمجية جديدة هنا بنطاق دقيق. |
